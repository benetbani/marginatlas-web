# Visual upgrade , all page types (2026-06-16)

A full visual upgrade of every Margin Atlas page type to the highest grade: a
premium commercial-SaaS look (Stripe/Linear polish), built from the owned
shadcnblocks block vocabulary + the re-skinned visx chart kit, on the warm Atlas
tokens, preserving the locked section orders and the honesty boundary.

## Read order
1. `00-ideology-and-design-law.md` , the law + the lead-designer QA bar (read first).
2. `01-component-and-chart-system.md` , the shadcnblocks registry, the ONE token
   map, the block-to-section menu, the chart strategy, the static-HTML delivery.
3. The six page specs:
   - `02-home.md`
   - `03-country.md`
   - `04-city.md`
   - `05-cell.md` (the flagship)
   - `06-neighbourhood.md`
   - `07-industry.md`
4. `08-build-sequence-and-qa.md` , the two phases, the wave order, definition of done.
5. `09-EXECUTION-PROMPT.md` , the paste-in prompt that executes the plan in sequence.

## The decision behind it
Settled in `../../../docs/superpowers/specs/2026-06-16-design-sourcing-decision.md`
and the design-sourcing memory: Supastarter shell (bought later) + shadcnblocks
Pro ($149, owned) + the free official shadcn v4 charts + the existing visx kit.
One token map themes the whole library at once; that is what makes upgrading every
page type tractable and hands-off (the AI names the blocks per page, composes,
wires the data, tones it; the founder approves whole pages).

## Status
Plan authored and ready. Execution NOT started, awaiting founder go. Phase A
(static HTML mockups for review) runs first, wave by wave; Phase B (porting into
the Next app on Supastarter) follows approval.

## The six page types (the canonical set)
Home, Country, City, Cell (activity-in-place), Neighbourhood (overview), Industry.
Region/state, neighbourhood-cell, learn, compare, and the directory hubs reuse the
same components and section spines, so they upgrade for free once these six land.
