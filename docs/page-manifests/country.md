# COUNTRY page manifest (e.g. /united-kingdom)

**Hero:** full-bleed masthead = country place photo + white-from-bottom gradient + faint world-map/locator motif on top at low opacity (texture, never a value) + flag chip + eyebrow ("Small-business economics, United Kingdom") + Newsreader country name + fixed subtitle + the answer line, with the at-a-glance 8-metric **table-card scorecard** riding the hero band as a frosted strip (each tile a one-word + position read; the two lead tiles , net wealth/adult and days-to-start , enlarged a step). **NO big hero number on this page type** , the one anchor figure (typical SMB tax ~19%) sits beside the country name as TEXT, not a Space-Grotesk hero numeral. The country is NOT a scored entity (cities are the only scored entity), so there is no /100 score and no `ScoreBand` hero; the masthead is the one full-width brand moment and the scorecard reads "vs global baseline, never a verdict."
**Section-id order:** `top` → `shape` → `setup` → `keep` → `hire` → `market` → `neighbours` → `ground` → `trend` → `cities` → `easiest` → `character` → `locals` → `fillingin` → `trust` → `take` → `close` → `compare`. (This IS the COUNTRY entry in `PAGE_SECTION_ORDER`.)
**Global rules per `00-README.md`** (palette = cool greys + terracotta only, no moss/amber/brown; fonts = Geist + Geist Mono + Newsreader + Space Grotesk; USD, abbreviated; bento half-width default; good/bad = calibrated word + position, never colour; viz from `00-global-standards.md` §6; no chart type >2x per page; redundancy law: one job per section).

---

## Section table , ONE ROW PER SUBSECTION, in order

Beat list authority: `page-architecture/00-APPROVED-REFORM-2026-06-18.md` "COUNTRY , reformed list (~22 real beats)" (items 1-24), used verbatim and in order. The beats are exploded into one row per subsection per `page-guidance/country.md` (§§1-18) and `page-architecture/analysis/country.md` (§§0-25 + the reformed re-ordered list). Jobs A-H per APPROVED-REFORM "eight customer jobs." Viz named from `00-global-standards.md` §6 (lines 211-231). Width per `00-BENTO-REFORM-PLAN` (half/col6 default). `Graphic` = exactly ONE named primitive from the closed set, or "none (stat tile / prose)". Format matches SPEC §4.2. Chrome rows (navbar, breadcrumb/map band, anchor rail, footer) are manifest boundaries, listed at the foot of this table for completeness, not numbered content beats.

| # | Section (id) | Subsection | Statistic | Graphic | Width | Data | Honesty |
|---|---|---|---|---|---|---|---|
| 1a | Hero (`top`) | name + anchor + the answer line | country name + flag + eyebrow + fixed subtitle + answer line ("the wage floor and the payroll on-cost, not the headline tax, decide what an owner keeps"); the one anchor = typical SMB tax burden ~19% set as TEXT beside the name | none (prose / Newsreader name; NO hero number, NO chart) | full | country name, flag, fixed subtitle, answer line, anchor tax % (held) | **No big hero number** (this page type carries none per `00-README.md`; only City + Cell do). Newsreader country name = 1 of 3 serif moments. Anchor is the cross-geography-SAFE figure (raw money cannot rank across borders). Caption surfaces the tax-vs-real-cost tension as a hook ("the headline number, and the one that matters least, here is why"). Anchor stated once at full weight (de-duped vs the setup `dl`, row 3b). |
| 1b | Hero (`top`) | at-a-glance 8-metric scorecard | 8 vital signs: GDP/capita ($49K), average salary ($44K), net wealth/adult ($172K, lead), days to start (4, lead), ease of business (83/100), minimum wage ($25K/yr), population (68.3M), cost of living (78/100) | stat-tile (frosted table-card scorecard cluster; two lead tiles enlarged + 6 supporting tiles, each one calibrated word + position) | full | mixed accessors (country-level) | USD, abbreviated ($49K not $49,000). Each tile a calibrated WORD + position (weak/fair/strong/excellent), never colour as carrier; NO moss/amber/cocoa tint (overrides the guidance doc's moss/cocoa/amber tile colours). Reads "vs global baseline, never a verdict" (a country is not scored). Population / min-wage / cost-of-living are NEUTRAL facts (no rung) , each must still earn a so-what (trivia filter). Lead tiles = net wealth ("can customers pay") + days to start ("can I get going"). |
| 2 | Country shape (`shape`) | seven structural lenses | 7 lenses , Reward, Cost to run, Ease of entry, People, Demand, Edge, Risk , each a one-word read + one-line why; the single best lens (Demand) carries the lone accent | diverging-spectrum-rows (one row per lens, worse-left to better-right, terracotta marker at the true position) | half | per lens: name, position 0..1, one-line why (modeled qualitative profile) | **Radar REJECTED** (a polygon reads as a composite score; cities are the only scored entity). NO polygon, NO aggregate, NO number. De-dupe vs the scorecard , this card is a qualitative PROFILE, not the metric values; cut it if it merely restates GDP/CoL/days/wages. 4-rung pips BANNED; spectrum only. Best lens = lone terracotta; all other markers neutral grey. "Profile, never a score." (Rated spectrum = a reused PRIMITIVE per BENTO §3, outside the capped chart-type count.) Pairs in its band. |
| 3a | Cost + rules to set up (`setup`) | register-and-trade stepper | the real multi-station flow: choose structure → register → register for tax/VAT → first-hire obligations; ~4 days typical, $0-$120 fee range | timeline-ribbon (horizontal multi-station stepper on a hairline) | full | steps + days + fee range $0-$120 (held) | **The page's spine and heaviest card** , full width earned (a genuinely wide stepper + table). The stepper MUST be a real multi-station flow, NOT a one-step fake (a single step is a stat, not a process). USD ($0-$120, never £). |
| 3b | Cost + rules to set up (`setup`) | held facts | business tax ~19%, payroll on-cost ~14%, time-to-go ~4 days, sales tax ~20% (customer-carried) | none (stat tile / `data-table1` row of figures, plain-words gloss each) | full | tax %, on-cost %, days, sales-tax % (held) | Sales-tax gloss = "the customer carries it, not the owner's burden" (misconception-correcting depth). Anchor tax % stated once at full weight HERE-or-hero, the other references it (`anchorPromotedFrom`). Payroll on-cost ~14% is REFERENCED here, canonical home = the hire bars (row 5b). |
| 3c | Cost + rules to set up (`setup`) | formation cost by tier | 3 tier cards, each "what it costs + who it suits": Sole trader (Free/$0, "fastest, simplest, you are the business"); Private limited company ($15 form / $120 all-in, "limited liability, the default for hiring"); Limited partnership ($15 / $180, "two-plus owners sharing the trade") | proportion-bar (grouped tier cards = labelled mini-cards, NOT a flat table; cost shown as a tiny computed magnitude per tier) | full | per tier: cost-to-form, all-in cost, who-it-suits (held) | USD. Grouped formation TIERS, not a flat table (the flat table was rejected as un-intuitive). Precise figures fold under a hairline in `data-table1` for the reader who wants exact numbers. Genuinely wide grouped block = a legitimate full-width exception (shares the masthead-class spine). |
| 4 | What an owner keeps (`keep`) | the illustrative take-home walk | one revenue→costs→tax→kept walk, all as % of $100; kept slice isolated + labelled; hard down-link to a trusted-local cell for the live figure | proportion-bar (100%-wide stacked startup bar, kept slice isolated and labelled) + companion read; link to `cell-london-restaurants.html` | half | revenue=100, cost segments, tax bite, kept slice (all as % of 100, computed to sum exactly 100%) | **No page above the cell prints take-home as if it owned it** (Job A owner = Cell). Clearly tagged "one illustration, the real number lives in a trade and a city." Prefer the 100% stacked bar over a donut (per §6 money rule; the donut override is Cell-only). Kept slice is the focal point and ALWAYS labelled; cost mass neutral. **NO moss on the kept slice** (palette override , kept slice carries the terracotta-or-position emphasis, not green). USD per $100. Geometry computed. |
| 5a | Hire + cost of a team (`hire`) | pay by level | wage floor (~$13/hr, ~$25K/yr), typical skilled (~$44K), senior (~$60K+) , each a row on ONE shared scale with the spread, median marked | dumbbell (junior/mid/senior floating-range rows on one shared scale, median = atlas dot) | half | per level: floor / median / ceiling (USD/hr + USD/yr) | All levels on ONE shared scale so senior is honestly longer (NEVER normalize away the gap). Lead with the floor-vs-typical spread (the surprising fact: "the legal floor is a fiction; budget the real rate"). USD. Talent folded in here (not a standalone beat). Pairs with 5b in the band. |
| 5b | Hire + cost of a team (`hire`) | employer on-cost vs neighbours | employer payroll on-cost (~14%) for UK vs IE/FR/DE/NL, subject = terracotta | ranked-bars/data-table1 (payroll on-cost comparison bars; subject UK terracotta, peers neutral, `noLeaderMark`) | half | on-cost % for UK + IE/FR/DE/NL (held) | **Canonical home of the payroll-on-cost figure (~14%)** , stated once at full weight HERE; the decisive `dl` (3b) and neighbours table (row 7) REFERENCE, not repeat (de-dup the three appearances). Peers NEVER get the terracotta accent (subject-only spotlight, no leader mark). Pairs with 5a. |
| 6 | The market you can reach (`market`) | reach + concentration + spending power | reachable population as CONTEXT (not a hero number) + concentration (share in top cities) + spending-power read blended from wealth + pay + local cost | one tightened positional band: spending-power located low->high as the single clear read; reach + concentration folded into ONE supporting context line (NOT three separate positional reads) , viz-fitness audit 2026-06-27 | half | reachable population (context), concentration (share in top cities), spending-power position 0..1 (modeled) | **Trivia filter:** raw "population 68.3M" as a lone big number is BANNED , this beat earns its tile ONLY by merging reach + concentration + spending-power into a decision read (absorbs "who has money" + "how far you reach"). **Cut entirely if it stays thin** (don't ship a stat dressed as a beat). No fabricated spend-mix split. Spending-power LOCATED on a band, never a vague word ("comfortable" banned). Population's one home = here (demoted from the scorecard's lone-figure role). Pairs in its band. |
| 7 | Versus the neighbours (`neighbours`) | the like-for-like facts table | per country (UK + IE/FR/DE/NL): business tax %, payroll on-cost %, cost-to-register (USD), time-to-register (days); rows LEAD with payroll | ranked-bars/data-table1 (zebra facts table; home column tinted terracotta-50 + left rule, `noLeaderMark`; optional inline magnitude bars per cell) | full | per country: business tax %, payroll on-cost %, cost-to-register (USD), time-to-register (days) (held) | The ONE legitimate country-vs-country comparison. Home column tinted, **NEVER crowned a winner** (`noLeaderMark`). Caveat travels in-card: "not adjusted for local prices, read each column on its own terms, not a league table." Rows ordered to LEAD with payroll (the figure that decides). USD for cost-to-register. Each neighbour column links to its country page (step-sideways nav). Genuinely wide table = a legitimate full-width exception. |
| 8 | The ground under you (`ground`) | operating-environment baseline | 2 held factors only: corruption read (0..100) + ease-of-operating read (0..100); sample factors CUT | stat-tiles (2 operating-baseline tiles, each a calibrated word + a tiny inline position dash) , visually distinct from the shape spectrum (viz-fitness audit 2026-06-27; was diverging-spectrum-rows, which echoed row 2 too closely) | half | corruption read (0..100), ease-of-operating read (0..100) (held) | **Sample factors CUT** , no hatched "no data" bars (a half-built section reads as broken). Only the 2 held factors ship. **Redundancy flag:** "ease of operating" ~ scorecard "ease of business" , if it merely restates the scorecard tile, MERGE into the scorecard cluster (row 1b) rather than ship a standalone card (the analysis doc recommends exactly this; it also drops one spectrum use). 4-rung pips banned; spectrum only. No semantic colour beyond the subject marker. (Rated spectrum = reused primitive, outside the capped count.) Pairs in its band. |
| 9 | How costs have moved (`trend`) | the cost trajectory | minimum-wage and/or employer-cost trajectory over recent years, direct-labelled "rising ~X%/yr" | gradient-area (single terracotta spark line, direct-labelled slope, stripped axis) | half | year series of minimum-wage (USD) and/or on-cost % (held/modeled) | Replaces the REJECTED flat "minimum wage rising steadily" statement (KILLED) , a sparkline of the actual path earns the tile because it shows the slope a low-pay model is fighting. ONE series only. Direct-label the slope; strip the axis. USD. Modeled/held tag as appropriate. **Chart-type watch:** this is spark #1; the slow-start beat (folded into the strip unless held) would be spark #2 , do not exceed 2x. Pairs in its band. |
| 10 | Cities (`cities`) | the uniform city grid | uniform city cards (London, Manchester, Birmingham, Edinburgh, Glasgow, Bristol, Leeds, Cardiff), one neutral non-ranking fact each + city-page link | data-table1 (uniform equal-weight card grid; no chart, no ranking) | full | per city: name, one neutral fact (metro population or region), city-page link | **A country NEVER scores its own cities** (cities are the only scored entity, and only on their own page). Uniform equal-weight cards, NEVER ranked. **Climate dots CUT** (3-for-all signals nothing) and the **duplicate chip row CUT** (the cards are already the links). One neutral, non-ranking fact per card so the grid informs without scoring. Primary forward path. Genuinely wide card grid = a legitimate full-width exception. |
| 11 | Easiest to break into (`easiest`) | backed activities + ungated chips | per backed activity: name + readiness read (position 0..1) + link to a trusted-local cell; other activities as plain "also commonly started here" chips | diverging-spectrum-rows (divided list: backed rows = readiness spectrum + cell link; ungated rows = plain chips) | half | per backed activity: name, readiness position 0..1, cell link; ungated: activity names (London-exemplar) | **Rank numbers DROPPED** until most rows carry a real readiness read (a 1-5 rank with no visible why reads as "ranked by vibes"). Ranks *activities WITHIN the country* (allowed), NOT places against places. Backed rows link to a trusted-local cell; ungated rows read "readiness fills in with a local cell." Spectrum for readiness, NOT pips. (Rated spectrum = reused primitive, outside the capped count.) Pairs in its band. |
| 12a | Character (`character`) | the two people-stats (lead) | 15% born abroad + 6% foreign-owned firms , the two hard people-stats leading the band | stat-tile (the 2 people-stats as lead tiles above the two rated tables) | half | % born abroad, % foreign-owned firms (held) | Lead with the two REAL people-stats (the most concrete thing in the section). USD n/a (counts/percents). These anchor the qualitative spectra that follow. |
| 12b | Character (`character`) | the rules, from a business view | 6 rated rows: clean-dealing, predictability, ease of contracts, red-tape, tax-system clarity, enforcement; best lens = lone accent | diverging-spectrum-rows (a `wide` table-card of oriented spectra, worse-left/better-right, terracotta marker every row) | half | per row (x6): pole labels + position 0..1 + optional one-line why (modeled) | **Two coloured rated tables , NEVER dropped or butchered to a pale strip** (founder: country character = the two 6-spectra tables; an agreed section that must not degrade). **No false-precision percents on subjective axes** (the slider-thumb-at-80% is the cringe) , qualitative position + a why tied to a held proxy. Anchor predictability ← corruption/ease (held), don't restate. 4-rung pips banned; spectrum. (Rated spectrum = reused primitive, outside the capped count.) Sits side-by-side with 12c forming one full band. |
| 12c | Character (`character`) | the culture, from an outsider view | 6 rated rows: welcoming, embraces-the-new, directness, formality, trust, pace; best lens = lone accent | diverging-spectrum-rows (a `wide` table-card of oriented spectra, worse-left/better-right, terracotta marker every row) | half | per row (x6): pole labels + position 0..1 + optional one-line why (modeled) | Same honesty as 12b , qualitative positions, no fake percents, best lens the lone accent. The two character tables (12b + 12c) sit side-by-side, each col6, forming one full band; distinct from the country-shape spectra (row 2) by being two grouped DENSE tables vs 7 single rows. (Rated spectrum = reused primitive, outside the capped count.) |
| 13 | What locals know (`locals`) | the insider beats | 4 real UK beats: PAYE is the slow step not registration; high-street rent understates true cost by ~a third (rates + service charge); small-premises rate relief varies a few miles apart; first hire triggers pension auto-enrolment, budget the on-cost from payslip one | severity-ladder (glyph-led list rows, one icon family; no chart) | half | 4 insider beats (icon + one-line each) (UK exemplar, hand-written) | **Promoted** , the signature human read a stats table can't give; the section that proves the product isn't a data dump. Glyph + one line; resist turning these into charts. ONE icon family only. Authored, tight (≤2 lines each). Exemplar-tagged (UK hand-written; other countries fall to the calm strip until filled). Slight overlap with the honest-take (rent>tax) is acceptable IF the two say escalating things. Pairs in its band. |
| 14 | Still filling in (`fillingin`) | the collapse strip | one calm strip absorbing: licences, cost-signature / where-the-margin-leaks, talent reality, opportunity gap, special zones, your-life-here , as anchor pills | none (collapse strip, `grey-100` wash; no chart, no sample cards) | full | list of unheld-section labels | **Anti-slop rule:** ALL unheld/degraded lenses collapse into ONE calm "still filling in" strip , NEVER separate sample cards posing as content, NEVER a dented chart, NEVER a fabricated number. This is the single home for licences + cost-signature + talent + opportunity + special-zones + your-life. Full width (it is itself the divider). World-map motif as quiet empty-state texture (decorative only). |
| 15 | How we get to the number (`trust`) | the trust layer | a short held-vs-modeled method/confidence note + per-section confidence flags | prose (small expandable method note, low contrast; no chart) | half | per-section held-vs-modeled flags; method blurb | Site-wide trust layer (monetizable per the approved reform). **No source-agency names** (gate `verify_no_source_agencies`). Quiet, low-contrast, no chart. Held vs modeled stated plainly. Pairs in its band. |
| 16a | The honest take (`take`) | the verdict | verdict line ("an easy place to start, a hard place to keep staff cheaply") + 2-3 held ticks | prose (calm accent panel, Newsreader verdict, narrow measure; no chart) | half | verdict line; 2-3 held ticks | The editorial verdict is the product. Newsreader = the verdict (2 of 3 serif moments). **Must say something ESCALATING** vs the hero answer line (1a) and the one-thing closer (17) , diagnosis here, forward instruction in 17, never the same thing thrice. Word + position, no colour. Pairs with 16b in the band. |
| 16b | The honest take (`take`) | one quick gut-check | three gut-check questions derived from THIS country's cost stack (can the customer pay your price; is there real margin after tax+payroll; have you tested demand before signing a lease) | none (three framed question cards, 3-up; no chart) | half | 3 derived questions | Questions derived from THIS country's actual cost structure, never boilerplate. Reads as a checklist, not a chart. Converts the reader from reading to deciding. Pairs with 16a. |
| 17 | One thing to remember (`close`) | the forward line | the single shareable forward instruction ("Cheap to open here, expensive to staff. Plan the business around the second hire, not the first day.") + freshness stamp + flag-it link | none (Newsreader closing line, the page's serif bookend; no chart) | full | closing line; last-checked date; flag-it link | **A forward INSTRUCTION, distinct from the honest-take's diagnosis** (16a says what's true; this says what to do). The one line a reader screenshots. Newsreader = the closer (3rd and last serif moment). Freshness + flag-it beneath = trust furniture. The one-thing closer is an allowed full-width exception. |
| 18 | Related / Compare CTA (`compare`) | the closing hand-off | "Set the United Kingdom side by side with up to three countries: revenue, the cost stack, what an owner keeps." + one "Open Compare" button | none (`cta10` calm panel, copy left + one button right; no chart) | half | CTA copy; Compare deep-link | The single closing hand-off zone , ONE terracotta CTA, not three link grids. Absorbs the cut "same business abroad" value (that is Compare's job). Names the exact payoff (cost stack + take-home), never "Explore more." One CTA only. Pairs in its band (or sits beside the closer). |

**Chrome (manifest boundaries, not scored beats):** global navbar (`navbar1`: logo + Countries/Industries/Cities/Compare dropdowns + inline search + one dark CTA) above row 1a; breadcrumb + thin lit world-map motif band (decorative SVG §5.1, single terracotta locator, never encodes a value) under the navbar; a sticky section-anchor rail (jump-to spine: At a glance / Country shape / Cost to set up / What you keep / Hire / Market / Vs neighbours / Cost trend / Cities / Easiest / Locals / Honest take) under the hero, given the long scroll; rich multi-column footer (`footer7`, ink-900 ground) below row 18.

**Thin-country degradation:** consecutive unheld sections collapse into the ONE calm "still filling in" strip (row 14) with section ids preserved as anchor pills; a genuinely filled section breaks the strip. Exemplar beats (locals, easiest, character) are UK hand-written; other countries fall to the strip until filled. A country is never scored and never scores its own cities, on any degradation path.

---

## Cross-page hand-offs (Country is the upstream fork in the geography spine)

- **DOWN** to city pages from every card of beat 10 (the primary forward path; London the deepest); the only route to a scored entity.
- **DOWN** to a cell page from beat 4 (what an owner keeps → the live take-home number) and every backed row of beat 11 (easiest to break into → a trusted-local cell). The country NEVER prints take-home as its own.
- **SIDEWAYS** to neighbour countries (IE/FR/DE/NL) from each column of beat 7's facts table (step-sideways nav).
- **OUT** to industry pages: the easiest-to-break-into rows and the cost-and-keep block each may link to the matching industry page.
- **OUT** to Compare from the navbar + the closing CTA (beat 18) + an inline "compare these four" affordance in beat 7.
- The ONE terracotta CTA on the page is the Compare / "Open Compare" pill; all other links are quiet ink→atlas text links (richly woven, no field of buttons).

---

## Footer , conformance config + notes / conflicts

### Conformance config (machine-checkable; extends `_qc.cjs` `CONFORMANCE`)

- **Section-id list (order):** `top`, `shape`, `setup`, `keep`, `hire`, `market`, `neighbours`, `ground`, `trend`, `cities`, `easiest`, `character`, `locals`, `fillingin`, `trust`, `take`, `close`, `compare`. Every id renders, in this order; none missing; no extras. (Chrome , navbar, breadcrumb/map band, anchor rail, footer , wraps but is not a content id.)
- **Currency:** USD ($) everywhere, abbreviated ($49K not $49,000). The legal strip must state USD.
- **Per-graphic max-count:** no chart TYPE more than 2x across all subsection rows (count below).
- **Banned graphics:** radar / radial vanity dial / polygon profile (a polygon reads as a composite score; country shape + risk use spectrum rows instead); pie / donut for the spend split, the owner-keeps walk, or any split (use the 100% stacked `proportion-bar`); 4-segment pip rows / segment meters (use the diverging-spectrum-row); raw-population stat-tile as a lone big figure (population is context inside beat 6 only); semantic good/bad colour (moss/amber) , good/bad is a calibrated word + position only; NO hero number (this page type carries none).
- **Width law:** full-width ONLY for the masthead (1a/1b), the cost-and-rules spine (3a/3b/3c), the neighbours wide table (7), the cities grid (10), the still-filling-in strip (14, itself the divider), and the one-thing closer (17). Every other band is half (col6) and carries 2+ cards , including owner-keeps (4), hire (5a/5b), market (6), ground (8), trend (9), easiest (11), character (12a-12c), locals (13), trust (15), honest-take (16a/16b), and the Compare CTA (18). No lone full-width section that could be half.
- **Font law:** Newsreader = exactly 3 serif moments (country name 1a, honest-take verdict 16a, closer 17). Space Grotesk = scorecard figures (1b) only (NO hero number on this page type). Geist Mono (tabular 500) on every figure.

### Chart-type repetition count (no type >2x; counted across the subsection rows above)

Counting only TRUE named charts (chrome, stat tiles, the scorecard, card grids, the stepper-ribbon, glyph lists, the collapse strip, and prose panels are not "charts"). The **diverging-spectrum-row is treated as a reused PRIMITIVE / atom** (per `00-BENTO-REFORM-PLAN` §3, which makes the rated spectrum a kit component, like a table or a card grid), NOT a named chart shape subject to the 2x rule , consistent with how city.md treats the gradient mini-spectrum. It is therefore listed but flagged separately, and differentiated by grouping + density so the cards never read as the same shape.

| Graphic (capped chart types) | Rows | Count | Status |
|---|---|---|---|
| `ranked-bars/data-table1` | 5b (payroll on-cost bars) + 7 (neighbours facts table) + 10 (cities grid, as a table) | **2 as a CHART (5b + 7); 10 is a non-charted card grid** | AT CAP as a chart , the payroll bars (5b) + the neighbours table (7) are the two; the cities grid (10) and the formation tiers use no bar geometry, so they do not push it to 3. Do not add a third ranked-bar chart. |
| `dumbbell` | 5a (pay by level) | **1** | — |
| `proportion-bar` | 3c (formation tiers, tiny magnitude) + 4 (owner-keeps 100% stacked) | **2** | AT CAP , do not add a third. 3c's per-tier magnitude is a light proportion atom; 4 is the 100% stacked walk. (If 3c reads as a chart and a third proportion-bar is ever needed, render 3c as plain labelled tier cards with no magnitude, keeping this at 2.) |
| `ScoreBand` | 6 (market reach band) | **1** | — (and §6 ScoreBand is also legitimately the vs-world grammar if that beat ever ships as its own card; it would reuse this grammar, not add a type) |
| `gradient-area` | 9 (cost trend spark) | **1** | — (a 2nd spark would be the slow-start beat; that beat folds to the strip unless held, so spark stays at 1, ceiling 2) |
| `timeline-ribbon` | 3a (register-and-trade stepper) | **1** | — |
| `severity-ladder` | 13 (what locals know, glyph rows) | **1** | — (glyph list, not a charted ladder of bars) |
| `donut+companion` | — | **0** | NOT used (the spend split / owner-keeps is a 100% stacked `proportion-bar`, never a donut/pie; the donut override is Cell-only) |
| `diverging-track` | — | **0** | not used (the country's diverging reads are spectrum ROWS, the reused primitive, not the City saturation diverging-track) |
| `vertical-bars` | — | **0** | not used |

**Reused PRIMITIVE / atom , OUTSIDE the capped chart-type count** (per BENTO §3, consistent with city.md's gradient-mini-spectrum treatment): the **diverging-spectrum-rows** appear in country-shape (2, seven single rows), ground (8, two rows), easiest-readiness (11, markers inside a divided list), and the two character tables (12b + 12c, two dense side-by-side tables). These are the honest replacement for every banned pip/radar quality-read and naturally recur; the source docs themselves push all of them onto the spectrum. They are kept visually consistent and differentiated by grouping + density so they never read as one repeated card. Also outside the count: the scorecard **stat-tiles** (1b, 12a), the formation **tier cards** (3c if rendered without magnitude), and the collapse-strip.

**To vary if a true CHART would exceed 2x:** if a strict reviewer counts the diverging-spectrum-rows as ONE named chart type (4 logical uses), **vary by (1)** merging row 8 (ground) into the scorecard cluster (1b) , the analysis doc recommends exactly this ("ease of operating ~ ease of business"), removing one use , and **(2)** rendering row 11 (easiest) as plain readiness CHIPS rather than a spectrum until more rows are backed , which drops the spectrum to 2 logical uses (country-shape + the two character tables, themselves one grouped treatment) and clears the gate cleanly. Never let `proportion-bar` or `ranked-bars` exceed 2: if beat 6 ever needs a stacked bar, keep beat 4 the only 100% stacked and beat 6 a ScoreBand.

### Redundancy-law check (no two sections serve the same job; a number stated once at full weight)

- **Scorecard appears twice** in APPROVED-REFORM , as a hero sub-element (1b) and as a standalone beat-5-equivalent. It is ONE cluster; render it ONCE in the hero band (1b). There is no second copy.
- **Anchor tax % (~19%)** , hero (1a) + the decisive `dl` (3b). State once at full weight (the decisive card), reference in the other (`anchorPromotedFrom`).
- **Payroll on-cost (~14%)** , the decisive `dl` (3b), the hire bars (5b), and the neighbours table (7) , three appearances. Canonical home = the hire bars (5b); the others reference, not restate.
- **Population** , scorecard (1b) and market reach (6). ONE home , it lives in reach (6) as context, demoted from a lone big number; the scorecard tile is a neutral fact, not a second feature.
- **GDP/capita** , scorecard (1b) and (if a vs-world band ships) the global-context ScoreBand , acceptable, the band is the contextualising home; not double-counted as a feature.
- **Ground "ease of operating" (8) ~ scorecard "ease of business" (1b)** , near-duplicate; if 8 merely restates the tile, MERGE into the scorecard cluster (also resolves the spectrum-count conflict above).

### Notes / conflicts , the current `country-uk-reform.html` VIOLATES this manifest (rebuild checklist)

The existing mockup (`E:\atlas\country-uk-reform.html`) was built before this manifest and breaks it on multiple axes. The rebuild MUST fix every item below:

1. **A non-protocol 6-spoke RADAR.** The "Economic profile" card (lines 302-318) renders a six-spoke radar polygon ("Reward / Afford / Ease / Talent / Edge / Stability," each scored 1-10). The radar is **explicitly REJECTED** for the country page (APPROVED-REFORM + guidance + analysis §3: a polygon reads as a composite SCORE, and cities are the only scored entity). **Fix:** delete the radar; replace with the country-shape `diverging-spectrum-rows` (row 2, seven lenses, worse-left/better-right, lone accent on the best lens), framed "profile, never a score."

2. **A DONUT for the spend split.** "How the customer spends" (lines 464-481) is a 5-slice donut (discretionary 35% / housing 30% / food 15% / essentials 12% / transport 8%). Donut/pie for a split is **banned** (§6 money rule: prefer the 100% stacked bar; donut is Cell-only). The whole "How the customer spends" beat is also not a manifest beat. **Fix:** drop the donut; the manifest's only money-split visual is beat 4's 100% stacked `proportion-bar` for the owner-keeps walk (kept slice labelled, no moss). Customer spending power lives in the market-reach band (beat 6) as a located read, not a spend-mix pie.

3. **GBP, not USD.** Every figure is in pounds (£50, £40, £44K, £25K, £200K, £2M, £140K, £52K, £95/sqft, etc.) and the legal strip states "Money values shown in GBP (£)" (line 881). Violates the locked currency rule (README precedence + reconciliation log 2026-06-27: USD). **Fix:** convert every figure to USD, abbreviated ($49K), and update the legal strip to state USD.

4. **Wrong scorecard contents + a "Corporation tax 25%" anchor.** The hero scorecard (lines 274-281) shows Corporation tax 25% / Capital gains 20% / Employer on-cost +14% / Set up in 4 days / Ease 83 / Cost of living Dear / Financing Active / Stability High , NOT the manifest's 8 vital signs (GDP/capita, average salary, net wealth/adult, days to start, ease of business, minimum wage, population, cost of living) and it leads on the wrong, non-cross-geography-safe "25% corporation tax." **Fix:** rebuild the scorecard to the manifest 8 (row 1b) with the two lead tiles enlarged (net wealth, days to start); the one anchor beside the name is the typical SMB tax ~19% as TEXT (row 1a), with the tax-vs-real-cost tension surfaced as a hook. No /100 score, no hero number.

5. **Missing manifest beats / wrong section spine.** The mockup's movements (Profile / Getting in / People / Money / Demand / Comparison / Trades / Operating / Risk & exit / Cities / Character / Close) do not match the manifest order and OMIT several required beats: the **country-shape spectrum** (row 2; replaced by the radar), **what an owner keeps** (row 4, the 100% stacked take-home walk hard-linked to a cell , entirely absent), the **market-reach ScoreBand** (row 6), the **cost-trend spark** (row 9), and **easiest to break into** (row 11). **Fix:** rebuild to the manifest's 18-beat spine in order (`top`→`shape`→`setup`→`keep`→`hire`→`market`→`neighbours`→`ground`→`trend`→`cities`→`easiest`→`character`→`locals`→`fillingin`→`trust`→`take`→`close`→`compare`).

6. **Out-of-spec extras with no beat (much of it premium-gated).** "Getting in and getting legal" (red-tape table + banking-if-foreign), "How easy it is to hire," "Talent & skills," "Financing & lending climate," "Payments & getting paid," "Company structures & banking in depth" (premium), "Where the money and the jobs are" (sector ranked bars), "Five common trades and what they cost to open," the entire **Operating & legal** premium movement (landlord terms, insurance cover, licensing, operator playbook, property in depth, compliance calendar), and **Risk & exit** (risk-out-of-10 KPIs + exit climate + export friction) are NOT country beats in the manifest. **Fix:** drop or fold them , the unheld/degraded ones collapse into the ONE "still filling in" strip (row 14); the genuinely country-level ones that survive map to existing beats (sector mix → not a beat; trades-to-open → industry/cell concern). Premium-gating is a monetization layer applied ON TOP of the manifest structure, never a substitute for a beat.

7. **Full-width where half is required (bento width law).** Single-card full-width bands that should be half-width tiles paired with a sibling: the export-friction table (line 720, col12) and , while the CTA closer at col12 is an allowed full-width exception , the export table is not. **Fix:** demote non-exception full-width single cards to half (col6) and pair them; reserve full-width for the masthead (1a/1b), the cost-and-rules spine (3a-3c), the neighbours table (7), the cities grid (10), the still-filling-in strip (14), and the one-thing closer (17) only.

8. **Cities grid carries a locator map + per-card pay/rent reads that lean toward ranking.** The cities movement pairs a locator SVG (lines 746-755) with city cards showing People/Pay/Rent (lines 761-764). The locator map is acceptable as decorative context (it never encodes a value), but the per-card Pay/Rent reads risk reading as a soft ranking, and the manifest requires ONE neutral, non-ranking fact per card. **Fix:** keep the uniform equal-weight grid with ONE neutral fact per card (metro population or region), no pay/rent comparison columns, no climate dots, no duplicate chip row; a country never scores or ranks its own cities.

### Section-constitution check (nothing agreed is dropped)

Cross-checked against `docs/brand/section-constitution.md` COUNTRY spine (24 items). Every constitution job survives in the 18 reformed beats: Hero+anchor → 1a; Scorecard → 1b; Country-shape (9-lens radar) → 2 (the 7-lens spectrum SUPERSEDES the rejected radar); Cost-and-rules-to-set-up → 3a/3b/3c; What-an-owner-keeps → 4 (a NEW keystone the answer line demands); Hire/team → 5a/5b (talent FOLDED in, not a standalone); The-market-you-can-reach → 6 (absorbs "who has money" + "how far you reach"); Versus-the-neighbours → 7; The-ground-under-you → 8 (sample factors cut, or merged into the scorecard); How-costs-have-moved → 9 (NEW, the honest replacement for the KILLED "momentum" lens); Cities → 10; Easiest-to-break-into → 11; Character (two 6-spectra tables) → 12a/12b/12c; What-locals-know → 13 (promoted); Trust layer → 15; Honest-take + gut-check → 16a/16b; One-thing-to-remember → 17; Compare CTA → 18. **Collapsed into the ONE "still filling in" strip (row 14), never silently dropped:** licences, cost-signature / where-the-margin-leaks, talent reality, opportunity gap, special zones, your-life-here. **Cut and accounted for:** "same business here vs abroad" (Compare's job, routed to row 18); standalone "talent" (folded into hire, 5a); "your life here" felt-bars (fabrication-prone, the concept covered honestly by locals, row 13). The "slow start" survival-runway beat is a conditional addition (folds to the strip unless a real time-to-trade-vs-breakeven read is held). The constitution's nine-lens country-shape radar is explicitly superseded by the 7-lens spectrum (radar rejected).

### Source citations per row

- Beats 1-18, list + order + jobs A-H + merges/cuts/demotions: `page-architecture/00-APPROVED-REFORM-2026-06-18.md` , "COUNTRY , reformed list (~22 real beats)" (items 1-24) + "The eight customer jobs."
- Per-subsection visual + honesty detail and the sub-section expansion: `page-guidance/country.md` (hero + scorecard §1 + the metric-rating system [1a/1b]; country shape §3 [row 2]; cost-and-rules §4 [3a/3b/3c]; what-an-owner-keeps §5 [4]; hire §6 [5a/5b]; market reach §7 [6]; neighbours §8 [7]; cost trend §9 [9]; cities §10 [10]; easiest §11 [11]; character §12 [12a/12b/12c]; locals §13 [13]; vs-world §14 [folds to ScoreBand/strip]; trust §15 [15]; honest-take + gut-check §16 [16a/16b]; one-thing §17 [17]; Compare CTA §18 [18]; collapse strip [14]).
- Reform rationale + what-was-wrong + the cuts (radar, donut, sample cards, duplicate population/payroll/ease reads): `page-architecture/analysis/country.md` (§§0-25 audits + VERDICTs; "What is MISSING" [new keep/slow-start/cost-trend beats]; "What to CUT or MERGE"; "The reformed, re-ordered section list").
- Viz names + geometry + honesty-per-statistic: `00-global-standards.md` §6 (lines 211-231) + §7 computed-geometry.
- Width / bento / five laws / the two-character-tables restoration: `00-BENTO-REFORM-PLAN-2026-06-18.md` §§2-5 (Laws 1-5; §4 country audit).
- Palette / fonts / USD / good-bad-as-word / no-hero-number / hero spec / no-chart>2x / redundancy law: `page-manifests/00-README.md` (global-rules block + precedence table + reconciliation log lines 60-62).
- Nothing-dropped audit: `docs/brand/section-constitution.md` COUNTRY spine (24 items) cross-checked against the rows above.
