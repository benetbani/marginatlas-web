# CELL page manifest (flagship: a trade in a place, e.g. Restaurants in London)

**Hero:** place photo + white-from-bottom gradient + Newsreader trade-in-place name + THE GAP (revenue-in vs take-home-kept) as the focal lead, with a distribution curve (p10/typical/p90) and a 5-stat decision scorecard (revenue, take-home, difficulty, break-even, payback) , NOT a generic country 8-tile scorecard.
**Section-id order:** `top` → `verdict` → `calculator` → `take` → `plain` → `demand` → `money` → `sensitivity` → `vs-salary` → `breakeven` → `risks` → `pay` → `getting-in` → `seasonality` → `exit` → `nearby` → `vs-world` → `method` → `one-thing` → `related`. (This IS the CELL entry in `PAGE_SECTION_ORDER`.)
**Global rules per `00-README.md`** (palette = cool greys + terracotta only, no moss/amber/brown; fonts = Geist + Geist Mono + Newsreader + Space Grotesk; USD; bento half-width default; good/bad = word + position; viz from `00-global-standards.md` §6; no chart type > 2x per page; redundancy law).

> Authoritative section list + order: the 20 "CELL , reformed list" beats in `page-architecture/00-APPROVED-REFORM-2026-06-18.md`. Per-section detail: `page-guidance/cell.md` + `page-architecture/analysis/cell.md`. Viz map: `00-global-standards.md` §6. No agreed section dropped (checked vs `brand/section-constitution.md`). Two founder overrides survive: (1) the money split is a **DONUT + 100% companion bar**, kept slice **terracotta or labelled grey, never moss**; (2) the seasonality beat (14) is a **gradient AREA chart** (area is correct here, unlike the city demand-calendar = vertical bars).

---

## Section table , ONE ROW PER SUBSECTION, in order

| # | Section (id) | Subsection | Statistic | Graphic | Width | Data | Honesty |
|---|---|---|---|---|---|---|---|
| 1a | Masthead (`top`) | trade-in-place name + breadcrumb | "Restaurants in London" + breadcrumb Home / GB / London / Restaurants | prose (Newsreader title + breadcrumb hand-offs) | full | trade_label, place_label, breadcrumb hrefs | Newsreader name (1 of 3 serif moments); breadcrumb segments are live upward hand-offs. |
| 1b | Masthead (`top`) | THE GAP + distribution curve | revenue-in (typical $503K) with the take-home echo directly beneath ($48K kept, ~10%); p10/typical/p90 ($252K / $503K / $905K) | `Newsreader-number+density-curve` (oversized revenue figure + take-home echo + density silhouette with computed typical marker) | full | revenue_typical, takehome_typical, net_margin_pct; revenue_low/typical/high | USD, abbreviated, rounded $1K. **Lead with the GAP (in vs kept), never revenue alone.** Density marker computed `(typical-low)/(high-low)`; curve label = "the spread of comparable rooms," never a probability. Space Grotesk only on the one hero number. |
| 1c | Masthead (`top`) | 5-stat scorecard | revenue, take-home, difficulty, break-even, payback, each with a read | `5-stat-tile-row` | full | takehome_typical, net_margin_pct, difficulty_band, breakeven_covers, payback_years | Good/bad = calibrated WORD (thin/slim/hard/slow) + position, no semantic colour. Tabular figures. |
| 2a | At-a-glance verdict (`verdict`) | the five decision numbers | revenue, take-home, difficulty, break-even, payback , one flat strip, each with its read | `5-stat-tile-row` (flat quiet strip, no image, no big serif) | full | same five as 1c | The reformed KPI row, NOT a second hero. Decisive numbers (take-home, margin) lead; read = word + position. Pairs with the masthead above it. |
| 3a | Calculator (`calculator`) | make-it-yours | covers/day x avg spend x days = revenue, minus rent / staff / owner's draw = take-home; a "count owner's draw as cost" switch; live result + the visible arithmetic chain | `calculator` (raised `--shadow-card` card; sliders + `switch` + live result line + "the arithmetic" readout; computed slider fills) | full | covers_per_day, avg_spend, rent_annual, staff_cost_annual, owner_draw; derived revenue + take-home | The one terracotta moment is the result line. **SHOW THE MATH** (the honesty differentiator). Mounts only when real take-home is held; otherwise folds to the collapse strip. |
| 4a | The honest take (`take`) | verdict + three levers | the editorial verdict line (what kind of business this is) + three levers, each a number or a mechanism | prose (accent panel `cta10`, buttons omitted; Newsreader verdict line = 1 of 3 serif moments) | half | verdict_text, lever_1/2/3 (number-or-mechanism) | The one warm-tinted prose card. Levers never platitudes. Distinct from #19 (diagnosis vs forward instruction). Pairs with 4b. |
| 4b | The honest take (`take`) | break-in difficulty | the break-in band anchored to a STATED peer set ("harder than most UK food trades") | `ScoreBand` (gradient/position track + word, marker at computed position) | half | breakin_band, breakin_peerset_label, breakin_position_pct | Break-in MUST name "compared to what." Links UP to the city page (the only scored entity). Position + word, NOT a 4-rung pip (banned). |
| 5a | In plain terms (`plain`) | the three physical facts | covers/day (~116), avg spend per cover (~$12), payroll headcount (~12) | `icon-unit-cards` (three cards, one icon family, atlas-50 chips , the page's only icon-unit grid) | half | covers_per_day, avg_spend_per_cover, payroll_headcount | USD. **Fix the avg-spend definition in-card** ("per cover, food and drink, before service") so a low $12 doesn't trip a knowledgeable reader. Modeled, tagged. Pairs with #6. |
| 6a | Who eats here (`demand`) | day-part or local-vs-visitor split | one canonical split (lunch/dinner OR local/visitor) | `split-bar` (single horizontal split, subject terracotta, mass grey) | half | demand_split, day_part_label | CUT-IF-THIN: a real SHAPE that changes siting/staffing, never "spend: High." Folds to the collapse strip if unheld. Pairs with #5. |
| 7a | Where the money goes (`money`) | per-$100 split + the depletion honesty | COGS 30, payroll 33, rent 15, other 12, kept 10 of $100; center "$10 kept of $100" | `donut+companion` (donut, kept slice emphasized + every slice labelled, PLUS a 100% companion stacked bar; computed arcs `value/100 x 360`, segments sum to 100) | full | cogs_pct, payroll_pct, rent_pct, other_pct, kept_pct (sum 100) | **Kept slice = terracotta OR labelled grey, NEVER moss.** Cost mass = neutral greys. The companion bar carries the depletion the donut hides , keep it. Kept slice is the focal point. "See the full method" links to #18. |
| 8a | Sensitivity (`sensitivity`) | what moves your margin most | top levers ranked by effect on take-home (+5% spend vs 5% rent cut vs one fewer cover-shortfall) | `ranked-sensitivity-bars` (one shared scale, mass grey, the single most-powerful lever terracotta) | half | lever rows: label + signed delta_to_takehome | **Sensitivity, NOT a cost re-rank** (the fix for the old bar-soup). A leverage chart, a new frame vs #7. One shared scale. Pairs with #9. |
| 9a | Owner vs salary (`vs-salary`) | the opportunity cost | take-home per hour AND take-home vs a comparable salaried role, each with a read | `stat-tile` (two figures, each with a gradient/position read , ahead of, or behind, a job) | half | takehome_per_hour, hours_per_week, comparable_salary, takehome_vs_salary_delta | USD. The most decision-changing comparison ($48K on 60-hour weeks running $350K of risk vs $48K salaried). Read = word + position. New frame on take-home. Pairs with #8. |
| 10a | Break-even (`breakeven`) | the threshold | how full you must be to stop losing money, wired to the calculator | `threshold-gauge` (position below/above the line; lone terracotta tick at break-even, neutral tick at the typical day, both labelled) | half | breakeven_covers (95), typical_covers (140), capacity_covers (180) | Below/above by POSITION + word, not amber/moss. Floor >= 2/day. "covers costs at ~95/day; a typical room runs ~140." Wired live to #3. Pairs with #11. |
| 11a | What to watch (`risks`) | trade-specific risks | 4 rows graded serious / watch / rare, each a calm note (rent reset on renewal; holding kitchen staff; a quiet-calendar stretch; hygiene / licensing / delivery-commission squeeze) | `severity-ladder` (3-bar glyph per row by fill + word + one-line note) | half | risk rows: label + severity_level + note | Every row specific to THIS trade in THIS place (cut any generic row). Neutral tone, never badmouth the trade. Severity = calibrated 3-level glyph + word, not stars. Pairs with #10. |
| 12a | Pay by role (`pay`) | levels by role + the owner's own pay | head chef / server / kitchen porter low-median-high on ONE shared $0-$80K scale; the owner's draw plotted on the same scale | `dumbbell` (shared-scale floating-range rows, median = terracotta dot; owner-draw marker the editorial twist) + `data-table1` for the figures | half | role rows: label + low/median/high; owner_draw; shared scale | USD. Shared scale stated in a caveat (a head chef's bar is honestly longer; never normalize to hide the gap). The punchline: the owner can earn less than their head chef. Median = the one terracotta mark per row. Pairs with #13. |
| 13a | Getting in (`getting-in`) | cost to open, itemized | fit-out 180K, kitchen kit 90K, deposits/legal 40K, opening float 40K = 350K total | `stacked-startup-bar` (computed segments summing to total, mass grey, terracotta total figure) | half | startup_total, fitout, kitchen, deposits_legal, float | USD. Pairs with the timeline below it in the same getting-in band (cost-out + when-it-turns = one money story). |
| 13b | Getting in (`getting-in`) | first-year phases | Mo 1-3 open, Mo 3-6 fragile (~30 in 100 don't survive), Mo 6-9 break-even node, Mo 9+ steady; payback ~6 years | `timeline-ribbon` (4-phase node row on a hairline; break-even node = lone terracotta) | half | phases[]; payback_years | **Survival figures stated as RANGES** ("about 30 in 100"), never false precision; de-numericize if not holdable. Nodes are phases, not precise dates. |
| 14a | Through the year (`seasonality`) | the 12-month cash-flow shape | trough Jan/Feb, peaks summer + December; busiest/quietest months; the buffer takeaway | `gradient-area` (single terracotta series, gradient fill, stripped axes, direct busiest/quietest labels) | full | month_index[12], busiest_month, quietest_month, cashflow_takeaway | **AREA is correct here** (unlike the city demand-calendar = vertical bars; 00-README override). One series; relative shape, not absolute. Direct-label the peak (Dec) + trough (Jan). Modeled, directional. "So what": a slow Jan/Feb can undo a thin-margin autumn , hold reserves. |
| 15a | Exit / valuation (`exit`) | what it could sell for | a valuation-multiple range (a small multiple of owner earnings OR revenue) + implied value range | `valuation-range` (RangeStrip family: typical marker + the honest band + a prominent caveat rail) | half | valuation_multiple_low/typical/high, multiple_base, implied_value_range | USD. NEW, founder-approved. **Hard caveat rail** , restaurant goodwill is thin; the fit-out, lease and location carry most of the price; never a promise. Modeled-and-tagged for the London exemplar. The asset, not just the wage. Pairs with #16. |
| 16a | The same business nearby (`nearby`) | like-for-like peers | London (subject) vs Edinburgh / Bristol / Manchester / Birmingham , same trade, same currency (London 503K=100% down to Birmingham 342K=68%) | `like-for-like-bars` (subject terracotta, peers neutral grey, computed widths; each peer row links to ITS OWN cell) | full | subject + peer rows: place_label + revenue; peer_cell_href each | USD, same trade, same currency, **not price-adjusted**. **Anti-ranking rail sacrosanct:** "read each on its own terms, not a league table." Subject = the only terracotta; peers never crowned. Districts suppressed. NEVER rank across business x geography. |
| 17a | Versus the world (`vs-world`) | global-median context | subject vs a global-median tick , ONLY when a real worldwide figure is held | `ScoreBand` (single track + global-median tick) OR the collapse strip | half | subject_value, global_median (only if held) | CUT-IF-THIN: a real global-median tick or NOTHING, never a fabricated world figure. Self-suppresses into the collapse strip. If it rarely holds for cells, demote to the industry page. Pairs with #18 when held. |
| 18a | How sure are we (`method`) | method + confidence | per-section confidence chips (held / modeled) + a one-line method note + a methodology link | `confidence-chips` (quiet expandable note + per-section held/modeled chips; no chart) | half | per_section_confidence[], method_note_text, methodology_href | NEW, site-wide trust layer. **No source-agency names.** The thing that makes the modeled figures defensible , the trust differentiator for the lender / advisor personas. Calm, low-contrast. Pairs with #17. |
| 19a | One thing to remember (`one-thing`) | the closer | the single shareable, forward-looking verdict | prose (Newsreader closing line, narrow measure = 1 of 3 serif moments) | full | closer_text | A FORWARD instruction, not a restatement ("a busy room, a thin margin , the lever is pricing power, not volume"). Distinct from #4's diagnosis (escalating, never the same thing twice). |
| 20a | Related + compare CTA (`related`) | the wayfinding zone | (a) other trades in this place (preview take-home each); (b) the same trade nearby (preview revenue each); (c) one Compare CTA; soft links up to Industry + Country | `data-bearing-link-tiles` + ONE terracotta "Compare two places" CTA (the page's only marketing button) | full | tile rows: trade_label + preview_takehome; place_label + preview_revenue; compare/industry/country hrefs | USD on every preview metric. Each tile carries a preview number (bare link grids are slop). ONE terracotta CTA. The page's only wayfinding zone (Job H), never three link grids. |

**Mechanisms (not numbered beats):** a **collapse strip** absorbs every unheld/sample section (esp. #6, #17, the calculator when take-home isn't held) into ONE calm "still filling in" strip , muted chips + one honest line, so a thin (non-London) cell never reads as a wall of dashes. **Chrome** , site-wide full navbar (Countries/Industries/Cities/Compare + search + one dark CTA) and the newsletter-forward dark footer with an elevated Methodology link.

---

## Footer , conformance config + notes / conflicts

### Conformance config (machine-checkable; extends `_qc.cjs` `CONFORMANCE`)
- **Section-id list (order):** `top, verdict, calculator, take, plain, demand, money, sensitivity, vs-salary, breakeven, risks, pay, getting-in, seasonality, exit, nearby, vs-world, method, one-thing, related`. Every id renders, in order; none missing; no extras.
- **Currency:** USD ($) everywhere, abbreviated.
- **Per-graphic max-count:** no chart TYPE more than 2x across all subsection rows (count below).
- **Banned graphics:** pie that hides the kept slice; moss/amber semantic colour (good/bad = word + position); 4-rung pip rows / segment meters; generic feature grids.
- **Width law:** full ONLY for the masthead (1a-1c), the verdict strip (2a), the calculator (3a), the donut band (7a), seasonality (14a), like-for-like (16a), the closer (19a), and the wayfinding zone (20a). Every other band is half (col6) and carries 2+ cards.
- **Font law:** Newsreader = 3 serif moments (trade-in-place name 1a, honest-take verdict 4a, closer 19a). Space Grotesk = the one hero number (1b) only. Geist Mono (tabular 500) on every figure.

### Chart-type repetition count (no type >2x; counted across the subsection rows)
| Graphic (capped chart types) | Rows | Count | Status |
|---|---|---|---|
| `Newsreader-number+density-curve` | 1b | **1** | hero |
| `5-stat-tile-row` | 1c + 2a | **2** | AT CAP |
| `ScoreBand` | 4b + 17a | **2** | AT CAP (break-in vs world-context , different jobs) |
| `ranked-sensitivity-bars` (true ranked bar) | 8a | **1** | + `like-for-like-bars` 16a = the ranked-bar FAMILY at **2**, at cap |
| `like-for-like-bars` (true ranked bar) | 16a | **1** | see above |
| `calculator` | 3a | 1 | |
| `icon-unit-cards` | 5a | 1 | |
| `split-bar` | 6a | 1 | |
| `donut+companion` | 7a | 1 | |
| `stat-tile` (vs-salary) | 9a | 1 | |
| `threshold-gauge` | 10a | 1 | |
| `severity-ladder` | 11a | 1 | |
| `dumbbell` | 12a | 1 | |
| `stacked-startup-bar` | 13a | 1 | |
| `timeline-ribbon` | 13b | 1 | |
| `gradient-area` | 14a | 1 | |
| `valuation-range` | 15a | 1 | |
| `confidence-chips` | 18a | 1 | |
| `data-bearing-link-tiles` | 20a | 1 | |
| `prose` | 1a, 4a, 19a | 3 | exempt (not a chart) |

The literal **horizontal ranked bar** appears only twice , #8 (sensitivity, signed deltas, mass grey + one terracotta lever) and #16 (like-for-like, subject terracotta + neutral peers) , at the 2x ceiling, carrying different jobs (G-leverage vs C-peers). The donut+companion (7), split bar (6), stacked startup bar (13a), dumbbell (12), and gauge (10) are stacked/floating/threshold shapes, NOT ranked bars, so they do not add to the count. **Watch item:** do not let 1c / 2a (stat tiles), 5a (icon units), and 20a (link tiles) all render as the same flat tile grid , vary their silhouettes. Density curve (1b) and gradient area (14a) are both filled-line silhouettes but are a distribution vs a 12-month series, sit far apart (Movement I vs IV), and read as distinct.

### Redundancy / duplicate-job check (the redundancy law)
- **Take-home (Job A) is stated many times by design, each a NEW frame:** the GAP (1b); the KPI commitment (2a); the reader's own scenario (3a); the per-$100 composition (7a); leverage on it (8a); opportunity-cost per-hour / vs-salary (9a); the asset/exit value (15a). "Stated once at full weight, later mentions add a frame" , compliant. The old "#2 In context" restatement and the bare "owner-keeps" re-statement are already cut by APPROVED-REFORM; not re-introduced.
- **Bar-soup on cost:** the donut (7a) is the ONE composition chart; #8a is a forward sensitivity, not a re-rank. Distinct jobs.
- **"Versus other places" (Job C) twice but different scopes:** #16a = same-trade nearby cities (owned peer view, anti-ranking); #17a = vs a global median (CUT-IF-THIN). Different scope, one self-omits , not a duplicate.
- **Difficulty / score (Job B) is borrowed, not owned:** the cell shows a break-in BAND (4b) and a break-even THRESHOLD (10a) , different questions , and hands the 0-100 score UP to the city page.

### Source conflicts resolved (per 00-README precedence)
1. **Donut vs waterfall:** `analysis/cell.md` recommends reverting to a waterfall; **APPROVED-REFORM founder override #1 + 00-README keep the DONUT + companion bar.** Followed the override.
2. **Kept-slice colour:** guidance/§6 say moss (`--chart-2`); **00-README no-moss rule supersedes** , terracotta or labelled grey. Same correction for every moss/amber read , good/bad = word + position.
3. **Seasonality:** area preserved for the cell (#14a) even though the city demand-calendar is vertical bars (00-README override).
4. **Currency:** GBP guidance/mockup -> **USD** (00-README 2026-06-27).
5. **Type/fonts:** global-standards' Inter + giant serif heads -> **bento small scale + Geist/Geist Mono/Newsreader/Space Grotesk** (BENTO §2 Law 3 + 00-README).
6. **Section order:** section-constitution (18, with "operator voices" + "in context") vs APPROVED-REFORM (20). APPROVED-REFORM wins; "operator voices" + "in context" intentionally cut (held / a 3rd restatement); demand (6), exit (15), method (18) ADDED, none dropped.

### Source citations
Per row: (1) `page-architecture/00-APPROVED-REFORM-2026-06-18.md` § CELL beat # (existence + order + viz hint); (2) `page-guidance/cell.md` § # (per-section detail); (3) `page-architecture/analysis/cell.md` § # (audit reasoning). Override sources (00-README / founder decision) named in the honesty note where they apply.

### Notes for the rebuild
Parts-bin base = `cell-london-reform.html` / `cell-london-restaurants-pilot.html` (hero shell, card frame, chart primitives, the calculator) , take structure/order/width/viz ONLY from this manifest. The calculator (beat 3) is the prototype of the paid what-if modeller. `section-order.ts` still holds the pre-reform `CELL_PAGE_SECTIONS` (7 ids); this manifest is upstream , P1 sync at reform ship.
