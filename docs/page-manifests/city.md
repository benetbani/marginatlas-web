# CITY page manifest (e.g. /cities/london)

**Hero:** full-bleed masthead = place photo + white-from-bottom gradient + country eyebrow/flag + Newsreader city name + the verdict sentence + the page's ONE hero number, the 0-100 **Business Climate Score** (Space Grotesk) on a `ScoreBand` with faint peer-city ticks (Paris 74 / Amsterdam 73 / Berlin 71 / Dublin 69), caption naming the one tension ("Deep demand, expensive ground"), plus the at-a-glance 8-metric **table-card scorecard** (score tile focal/atlas-tinted, seven supporting tiles each with a one-word calibrated read). City is the ONLY scored entity, so this number is load-bearing and gets the page's single biggest type; raw population is NOT a hero tile (one context line only).
**Section-id order (locked by the 20-question interview, 2026-06-27):** `top` → `take` → `customer` → `rent` → `map` → `where` → `field` → `keep` → `open` → `hiring` → `locals` → `changing` → `peers` → `close`. Six chapters: **Answer** (top, take) · **Customer** (customer: spending / footfall / POPs / the year) · **Place** (rent, map, where, field) · **Money** (keep, open, hiring, locals) · **Context** (changing, peers) · **Close** (close). ADDED vs the first pass: `rent`, `map` (a clear interactive-map placeholder until MapLibre ships, no fake stand-in), `hiring`, `locals` (gold-mine intel). DROPPED: the break-even ramp (belongs on the cell). Operator voices DEFERRED (held for real quotes). Density = almanac (every band 2+ cards). (This IS the CITY entry in `PAGE_SECTION_ORDER`.)
**Global rules per `00-README.md`** (palette = cool greys + terracotta only, no moss/amber/brown; fonts = Geist + Geist Mono + Newsreader + Space Grotesk; USD, abbreviated; bento half-width default; good/bad = calibrated word + position, never colour; viz from `00-global-standards.md` §6; no chart type >2x per page; redundancy law: one job per section).

---

## Section table — ONE ROW PER SUBSECTION, in order

Beat list authority: `page-architecture/00-APPROVED-REFORM-2026-06-18.md` lines 85-97 ("CITY — reformed list, 10 beats, the only scored entity"). The 10 beats are exploded into one row per subsection per `page-guidance/city.md` (§§ as cited) and `page-architecture/analysis/city.md`. Viz named from `00-global-standards.md` §6 (lines 211-231). Width per `00-BENTO-REFORM-PLAN` (half/col6 default). `Graphic` = exactly ONE named primitive from the closed set, or "none (stat tile / prose)". Format matches SPEC §9.

| # | Section (id) | Subsection | Statistic | Graphic | Width | Data | Honesty |
|---|---|---|---|---|---|---|---|
| 1a | Hero (`top`) | the verdict + the score | Business Climate Score 0-100 (78) + faint peer ticks (Paris 74 / Amsterdam 73 / Berlin 71 / Dublin 69) + peer-median tick; verdict sentence the score generates | `ScoreBand` (atlas subject marker, faint ink peer ticks, grey→terracotta ramp; number in Space Grotesk) | full | modeled, city-level Climate Score; peer scores modeled | only cities are scored; caption ALWAYS names the one tension ("Deep demand, expensive ground"); "same 0-100 scale, only cities are scored." Thin-city: score demotes to a quiet break-in chip, never a confident /100. No radial (a lone radial gives no peer context, reads as a vanity dial). |
| 1b | Hero (`top`) | at-a-glance scorecard | 8 vital signs: Climate Score (focal); median resident income ($52K); net wealth / adult ($220K, or swap to disposable / cost-adjusted spending power per open Q1); commercial rent character (132 vs 100); cost of living (78/100); demand depth (Deep); money to open ($90K–$260K); months to break even (14–22) | stat-tile (locked lead-plus-supporting cluster: score tile focal/atlas-tinted + 7 supporting tiles, each one calibrated word) | full | mixed accessors (city-level) | USD, abbreviated. Each tile one WORD read (weak/fair/strong/excellent for more-is-better; friendly/fair/dear/steep/slow for cost/friction), never colour as the carrier — position+word+size. Raw population is NOT a tile (one quiet context line only — trivia filter). |
| 2a | Honest take (`take`) | the verdict line + falsifiable bullets | a Newsreader verdict line + up to 3 metro-specific, falsifiable bullets | prose (calm accent panel, shadcnblocks `cta10` shape, buttons omitted; Newsreader verdict line = 1 of 3 serif moments) | half | London-exemplar; verdict-only on thinner cities | No chart. No platitudes ("location matters" banned); every bullet falsifiable AND city-specific or it is cut. Distinct from beat 10's close (framing up front vs warm last word — redundancy law). Pairs with a sibling card; never a lone full-width prose strip. |
| 3a | Customer (`customer`) | who pays (the band hero) | median resident income ($52K) + p10 / median / p90 income spread + one operator-legible second stat (disposable income OR cost-adjusted spending power) | `RangeStrip` (cocoa span, lone atlas median tick at computed position) | half | income spread (London-real) | USD. Led by the so-what ("a broad middle plus a deep luxury tail, so both value and premium can find a base"), not the bare number. Spread is London-real; RangeStrip self-omits elsewhere, the two stats still carry the band. |
| 3b | Customer (`customer`) | what that income buys | relative cost-of-ground character: national norm = 100, this city = 132, prime street ~180 | relative mini-scale (two-ended positional, national norm anchored; NOT a RangeStrip, NOT a pie) | half | modeled relative index | USD context. Explicitly labelled RELATIVE and "not a rent quote." Lead with the surprising prime-vs-side-street gap, not the bare 132. Modeled, always rendered. (Positional atom — outside the capped chart-type count.) |
| 3c | Customer (`customer`) | resident vs visitor | residents / visitors footfall split (72% / 28%) | `proportion-bar` (resident slice atlas-accented, never a pie) | half | modeled footfall split | "a rough share of footfall, not spend" (load-bearing — readers will mis-read 72% as revenue). Headline carries the implication ("mostly a residents' city, build for repeat trade"), not the bare split. Modeled, always rendered. |
| 3d | Customer (`customer`) | the year (steady vs seasonal) | footfall index by month (peak vs trough) | `vertical-bars` (half width — founder override, NOT an area chart) | half | modeled monthly pattern | Directional, not absolute; folded INTO the customer band as the steady-vs-seasonal read, never its own full-width movement. Area chart is the Cell seasonality idiom, not City (reconciliation log 2026-06-27). |
| 3e | Customer (`customer`) | who's around, by life-stage (POPs) | the resident population mix by the fixed life-stage archetypes (young professionals, families, established earners, students, older, lower-income) + visitors as a flow multiplier; a spending-power read per band | `POPs-life-stage-donut` (single ring, dominant band terracotta + graded greys, never moss; visitors as a separate +Nx flow chip, never inside the 100%) + legend with spend reads | half | modeled per-place composition mix (`PRODUCT-DIRECTION.md` section 14, ratified 2026-06-23) | The ratified ownable population layer, a CITY + NEIGHBOURHOOD feature. Visitors NEVER inside the donut. Lower-income / Other can never be the dominant fill. Describe-never-judge: every band = archetype + share + spend read, no evaluative adjectives. Confidence-tagged. ADDED 2026-06-27 , this layer was missing from the first manifest pass (founder caught it); it is a hard requirement, not optional. |
| 4a | Owners keep (`keep`) | the take-home menu across trades | per trade: net margin %, after-tax owner take-home as a typical→top RANGE, break-in difficulty word, cell href, optional industry href. London rows: dental 18% $95K moderate; law 22% $88K hard; accountants 21% $72K moderate; restaurant 9% $48K hard; cafe 7% $34K easier | `ranked-bars/data-table1` (`OwnerKeepTable`: bars encode take-home on one shared computed domain — top trade = 100%, others scaled; tabular margin column; break-in chip as WORDS; each row links to its cell) | full | per-trade cell-engine numbers, trusted-local only | USD, take-home as a typical→top RANGE (never one guaranteed number). **Anti-ranking caveat sacrosanct and visible:** "ordered by take-home, not by what is best; different trades, read each on its own terms." Must read as "different games," not a leaderboard. NEVER rank across business × geography. Self-omits below 3 rows. Break-in chip = the cell hand-off (the deepest, most valuable link). Genuinely wide table = a legitimate full-width exception. |
| 5a | What it takes to open (`open`) | capital-to-open | typical money-to-open low / mid / high ($90K–$260K) with atlas typical marker | `RangeStrip` (cocoa span, atlas typical marker) | half | modeled money-to-open | USD. Clearly **tagged modeled**, never false precision. Directly answers the hero's "if you can fund a slow start." Pairs with beat 4: what you keep vs what you put in. |
| 5b | What it takes to open (`open`) | months to break even | months-to-break-even range (14–22), phases not precise dates | months-to-break-even ribbon (timeline/stepper, break-even node = lone atlas emphasis) | half | modeled break-even | USD/time. Tagged modeled. Break-even node is the lone atlas tick; nodes are PHASES, not dates. (Phase ribbon — outside the capped chart-type count.) Sits in the same band as 5a (the open band carries 2 cards). |
| 6 | Saturation (`field`) | how crowded the field is | per trade: density-vs-norm read (over-supplied / balanced / white space), modeled | `diverging-track` (locked L12 diverging bars on a balanced-market centre line: over-supplied extends one way in cocoa, under-served the other, subject trade in atlas) | half | modeled per-trade density character | Framed as a density CHARACTER, never a precise count, never a cross-city ranking. Modeled, tagged. Diverging geometry (not clamped identical bars, which read as fabricated; and keeps clear of the ranked-bars count). Pairs in its band. (Open Q2: coarse over/balanced/under is honest enough to ship; if data is too thin, fold to per-trade chips on the beat-4 rows instead.) |
| 7 | Where to set up (`where`) | the suits-shortlist + neighbourhood hand-off | per district: suits-trade, relative rent read, relative footfall read, why-line, neighbourhood href. Soho (restaurants/nightlife, rent high, footfall high); The City (professional, rent high, footfall weekday-skewed); Shoreditch (cafes/creative, rent mid-rising, footfall mid-high); Mayfair (luxury/advisory, rent top, footfall mid). "Explore all neighbourhoods" tail | `suits-shortlist-divided-list` (border-top/bottom ROWS, not a card grid; suits pictogram from the one icon family — fork / briefcase / cup / star; tiny rent + footfall gradient mini-spectra, L5 grey→terracotta marker) | full | per-district relative reads, London-exemplar | Rent/footfall reads explicitly "relative within London." **4-segment pips BANNED — use the gradient mini-spectrum.** This single merged band IS both the prescriptive shortlist AND the neighbourhood links (redundancy law — neighbourhoods and a separate shortlist must NOT both exist). "Explore all neighbourhoods" tail carries the old navigation job. Wide divided list, distinct shape from grids = a legitimate full-width exception. Collapses to the calm strip elsewhere. |
| 8 | How the city is changing (`changing`) | forward read | insight verdict line + 3 trend rows, each a direction (rising / flat / falling) | `up-down-rail` (rising = word "up" / falling = word "down" + position, shared icon family; preceded by a ContrarianInsight verdict line) | half | London-exemplar momentum read | No chart (a chart would over-claim precision on a soft trend). **Highest fabrication risk on the page** — renders ONLY where a real, citable shift is held; self-omits aggressively. "Our read, not a forecast" register. DEMOTED below the action sections (beats 4-7) so an invented trend never sits high on a thin page. Up/down by WORD + position, not colour. Pairs with a sibling in the band. |
| 9a | Peers (`peers`) | the ranked comparison | subject + peers on the shared 0-100 scale (London 78, Paris 74, Amsterdam 73, Berlin 71, Dublin 69) | `ranked-bars/data-table1` (ComparisonBars: bar width = score, London the lone atlas accent, peers cocoa) | half | modeled city scores on the shared scale | The ONLY place a leader mark is honest (cities on the identical 0-100 scale). Caveat rail load-bearing: "only cities are scored; not adjusted for local prices; read it as a relative climate signal." London the lone atlas; never give a peer the accent. Self-omits below 2 peers. Shares the band with 9b (two visuals only — VsWorld median strip DROPPED as a redundant third take). |
| 9b | Peers (`peers`) | step-sideways nav | per peer: flag, score, city href | `peer-cards` (CityPeers step-sideways cards: flag + score + link) | half | per-peer city links | Lateral nav (the one legitimate sideways move). Peers neutral cocoa, never the accent. Shares the band with 9a. |
| 10a | Close (`close`) | the trust layer | held-vs-modeled method note + per-section confidence chips (held / modeled / exemplar) | prose (compact expandable method note + per-section confidence chips) | half | confidence/method note (no source-agency names) | No source-agency names (gate `verify_no_source_agencies`). Held vs modeled, makes the honesty visible. Quiet, low-contrast. Shares the band with 10b. |
| 10b | Close (`close`) | the one thing to remember | the one closing sentence + June 2026 freshness stamp + "flag it" affordance | prose (`OneThing` quiet close card; Newsreader closing line = 1 of 3 serif moments) | full | the one closing sentence | Newsreader close. Must be DISTINCT from beat 2's honest-take and forward-looking (London: "London gives you the demand; whether you keep any of it depends on the rent deal and the price you can hold"). Quietest card; the closing serif line MAY take the page's one full-width closer. Freshness + "flag it" = the honesty posture. |

**Chrome (manifest boundaries, not scored beats):** global navbar + breadcrumb (Home / Cities / London) above row 1a; rich multi-column footer (shadcnblocks `footer7`, dark ink-900) below row 10b. A sticky section-anchor rail is optional given only 10 beats.

**Thin-city degradation:** consecutive unheld sections (honest-take detail, saturation, where-to-set-up, changing) collapse into ONE calm "still filling in" strip with section ids preserved as anchor pills; a genuinely filled section breaks the strip. On a thin city the score demotes to a quiet break-in chip, never a confident /100.

---

## Cross-page hand-offs (City is the hub of the geography spine)

- **UP** to the country page from the breadcrumb + country eyebrow (London → United Kingdom).
- **DOWN** to cell pages from every row of beat 4 (break-in chip → the trade's cell; the deepest, most valuable hand-off); **DOWN** to neighbourhood pages from every row of beat 7 + the "Explore all neighbourhoods" tail.
- **SIDEWAYS** to peer cities from beat 9b's step-sideways cards.
- **OUT** to industry pages: each trade name in beat 4 may link to its industry page.
- **OUT** to Compare from the navbar + a closing CTA.
- The ONE terracotta CTA on the page is the Compare / "Get the data" pill; all other links are quiet ink→atlas text links (richly woven, no field of buttons).

---

## Footer — conformance config + notes / conflicts

### Conformance config (machine-checkable; extends `_qc.cjs` `CONFORMANCE`)

- **Section-id list (order):** `top`, `take`, `customer`, `rent`, `map`, `where`, `field`, `keep`, `open`, `hiring`, `locals`, `changing`, `peers`, `close`. Every id renders, in this order; none missing; no extras. (Matches the locked Section-id order header above; the `_qc.cjs` machine gate enumerates the 13 in-page section ids — this list minus the hero masthead `top`.)
- **Currency:** USD ($) everywhere, abbreviated ($52K not $52,000). The legal strip must state USD.
- **Per-graphic max-count:** no chart TYPE more than 2x across all subsection rows (count below).
- **Banned graphics:** radar / radial vanity dial; pie / donut for the visitor split or any split; raw-population stat-tile (population is one context line only); 4-segment pip rows / segment meters (use the L5 gradient mini-spectrum); semantic good/bad colour (moss/amber) — good/bad is a calibrated word + position only.
- **Width law:** full-width ONLY for the masthead (1a/1b), the owners-keep wide table (4a), the where-to-set-up divided list (7), and the one closer (10b). Every other band is half (col6) and carries 2+ cards. No lone full-width section that could be half.
- **Font law:** Newsreader = exactly 3 serif moments (city name 1a, honest-take verdict 2a, closer 10b). Space Grotesk = the one hero score number (1a) only. Geist Mono (tabular 500) on every figure.

### Chart-type repetition count (no type >2x; counted across the subsection rows above)

| Graphic (capped chart types) | Rows | Count | Status |
|---|---|---|---|
| `ScoreBand` | 1a | **1** | hero only |
| `RangeStrip` | 3a (income spread) + 5a (money-to-open) | **2** | AT CAP — do not add a third (3b is a distinct two-ended relative mini-scale, NOT a RangeStrip) |
| `ranked-bars/data-table1` | 4a (take-home bars) + 9a (ComparisonBars) | **2** | AT CAP — beat 6 saturation is a `diverging-track` (different geometry) to stay clear of this count |
| `diverging-track` | 6 | **1** | — |
| `vertical-bars` | 3d (the year) | **1** | — |
| `proportion-bar` | 3c (visitor split) | **1** | — |
| `suits-shortlist-divided-list` | 7 | **1** | — |
| `peer-cards` | 9b | **1** | — |
| `up-down-rail` | 8 | **1** | — |
| `dumbbell` | — | **0** | not used |
| `donut+companion` | — | **0** | not used (the visitor split is a `proportion-bar`, never a donut/pie) |

Positional / phase / atom graphics OUTSIDE the capped chart-type count (consistent with the gradient mini-spectrum treatment): the **relative mini-scale** (3b), the **months-to-break-even ribbon** (5b), the rent/footfall **gradient mini-spectra** (inside row 7), and the scorecard **stat-tiles** (1b). These are inline rating/positional atoms, not charted "types"; keep them visually consistent and do not let them proliferate into a third bar-soup.

**To vary if a beat changes:** if beat 6 saturation can only ship as ranked bars (not diverging), it would push ranked-bars to 3 — in that case render saturation as per-trade chips on the beat-4 rows (the §6 fallback) instead, keeping ranked bars at 2. Never let `RangeStrip` or `ranked-bars` exceed 2.

### Notes / conflicts — the current `city-london-reform.html` VIOLATES this manifest (rebuild checklist)

The existing mockup (`E:\atlas\city-london-reform.html`) was built before this manifest and breaks it on multiple axes. The rebuild MUST fix every item below:

1. **Missing beat 4 "What owners keep across trades."** The single most monetizable, decision-grade section (ranked take-home + margin + break-in chip + cell link) is absent; the page jumps from the customer band to a cost-to-open dumbbell with no take-home menu. **Fix:** add beat 4 (row 4a) as a full-width `ranked-bars/data-table1` (`OwnerKeepTable`) with the sacrosanct anti-ranking caveat; it is the spine after the score.

2. **Duplicated district treatments instead of the merged beat 7.** The HTML carries BOTH a "Neighbourhoods" chip row (line 531-536, col12) AND a separate "A starter shortlist" card (Movement 8, line 582-599) — the neighbourhoods-vs-shortlist duplicate the redundancy law and beat 7 forbid. It also has a third district artefact (the "Where the money clusters" map + "Rent by area" bars). **Fix:** collapse to ONE merged "Where to set up" `suits-shortlist-divided-list` (row 7) with per-row rent/footfall gradient mini-spectra + neighbourhood links + an "Explore all" tail; drop the standalone chip row and shortlist card.

3. **A non-protocol "wins/loses radar + red flags."** Movement 1 (line 337-375) renders a 6-spoke radar polygon plus a "Red flags, honestly" risk table. The radar is NOT a CITY beat, NOT in §6's viz map, and was explicitly REJECTED for the country page (APPROVED-REFORM line 64). **Fix:** delete the radar; the verdict is carried by the hero `ScoreBand` (1a) + the honest-take (2a). Risk content, if kept, folds into the honest-take bullets, not a separate severity table (City has no "what to watch" beat — that is a Cell beat).

4. **GBP, not USD.** Every figure is in pounds (£52K, £90K, £120/sq ft, etc.) and the legal strip states "All values shown in GBP" (line 647). Violates the locked currency rule (README precedence + reconciliation log 2026-06-27: USD). **Fix:** convert every figure to USD, abbreviated ($52K), and update the legal strip.

5. **Full-width where half is required (bento width law).** Multiple single-card full-width bands that should be half-width tiles paired with a sibling: the demand calendar (line 413, col12), cost-to-open (line 470, col12), the shortlist (line 585, col12). The BENTO law forbids "a lone full-width section that could be half" and requires every band to carry 2+ cards. **Fix:** demote these to half-width (col6) and pair them; reserve full-width for the masthead (1a/1b), beat 4's wide table (4a), beat 7's wide divided list, and the one closer (10b) only.

6. **Demand calendar is an AREA CHART, not the required vertical bars.** The HTML draws the demand calendar as a gradient area/line chart (line 415-421). The reconciliation log (2026-06-27, founder override) locks the CITY demand-calendar viz = **`vertical-bars` at half width** — the area chart is correct only for the Cell seasonality beat. **Fix:** render the demand calendar as `vertical-bars` at half width, folded INTO beat 3's customer band as the steady-vs-seasonal read (row 3d), not as its own full-width movement.

7. **Premium/paywall gating not in the manifest.** Several sections are wrapped in `.gate`/`.prem` paywall overlays (saturation, hiring, rent-by-area, momentum, shortlist), currently disabled by an override block (line 248-253) but the markup remains. Gating is a monetization concern, not part of the section/viz spec. **Fix:** the rebuild follows the manifest's section + viz structure; any paywall treatment is layered on top, not a substitute for a beat.

8. **Out-of-spec extras with no beat.** "Can you hire here" (a labour table, Movement 3), "Where the money clusters" (a bespoke London map, Movement 5), and a free-standing "A quick gut check" table (Movement 9) are not CITY beats. **Fix:** drop or fold them — hiring is a country-page concern (the country owns the team-cost beat); the map is not a CITY beat; the gut-check is a country-page device. Keep only the 10 beats + chrome.

### Section-constitution check (nothing agreed is dropped)

Cross-checked against `docs/brand/section-constitution.md` CITY spine (12 beats). Every constitution job survives in the 10 reformed beats: Hero+score → 1a; Scorecard → 1b; Who-the-customer-is + What-space-costs + Tourist-vs-local → merged into 3a/3b/3c (+ the steady-vs-seasonal 3d); What-owners-keep → 4a; Best-areas + Neighbourhoods → merged into 7; How-the-city-is-changing → 8; Rival-and-peer-cities → 9a/9b; One-thing-to-remember → 10b. **Operator voices** (constitution §11) is intentionally NOT a beat (held until real quotes exist — same posture as the Cell spine's cut); it is the one constitution item deferred by the honesty rail, not silently dropped. The NEW beats 5 (cost-to-open / ramp, Job E) and 6 (saturation) are additions the persona brief demands, not removals.

### Source citations per row

- Beats 1-10, list + order + merges + demotion: `page-architecture/00-APPROVED-REFORM-2026-06-18.md` lines 85-97.
- Per-subsection visual + honesty detail and the sub-section expansion: `page-guidance/city.md` (hero §§ lines 9-33; beat 1 §1; beat 2 §2; customer §3 + merge §4 [3a/3b/3c/3d]; saturation §5 [row 6]; owners-keep §6 [4a]; cost-to-open §7 [5a/5b]; where-to-set-up §8 [row 7]; changing §9 [row 8]; peers §10 [9a/9b]; trust layer §11 [10a]; close §12 [10b]; footer §13; thin-city line 133).
- Reform rationale + what-was-wrong + the cuts (radial, VsWorld, neighbourhoods/shortlist merge): `page-architecture/analysis/city.md` (audit §§; "What was WRONG" / VERDICTs; cuts/merges lines 122-127; reformed list lines 129-142).
- Viz names + geometry + honesty-per-statistic: `00-global-standards.md` §6 (lines 211-231) + §7 computed-geometry.
- Width / bento / five laws: `00-BENTO-REFORM-PLAN-2026-06-18.md` §2 (Laws 1-5) + §5 targets.
- Palette / fonts / USD / good-bad-as-word / demand-calendar-vertical-bars override / hero spec: `page-manifests/00-README.md` (global rules block + reconciliation log lines 60-62).
- Nothing-dropped audit: `docs/brand/section-constitution.md` CITY spine.
