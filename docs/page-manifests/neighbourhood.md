# NEIGHBOURHOOD (district) page manifest (e.g. /cities/london/west-end)

**Hero:** full-bleed district **photo** + white-from-bottom gradient scrim (legible cream base, never white text floating on a busy photo) + **Newsreader district name** (H1) + an answer-first two-marker verdict line (top trade AND most-squeezed trade) + the trade-labelled Newsreader hero figure ("Hotels 2x+", a MULTIPLIER, never a money figure, never a 0-100 district score) + the **lift-vs-squeeze two-marker multiplier gauge** (0.4x..3.0x track, city baseline pinned at 1.0x in ink, lift marker right in terracotta, squeeze marker a hollow grey dot left of baseline, hatched "clamped beyond here" band past ~2.0x) + a "relative read by design" note + a three-weight character pill row (price tier / economic tags / one character word). One focal point: the trade-labelled hero figure; the gauge is the proof beneath it. No hero CTA button (the clickable trade rows below are the CTA).

**Section-id order** (this IS the page's entry in `PAGE_SECTION_ORDER["neighbourhood"]`, upstream of `src/lib/page-layout/section-order.ts`, which still carries the pre-reform 2-section stub `hero, neighborhood-industries` and must be synced at reform ship, P1):
`navbar` (chrome) -> `headline` -> `honest-take` -> `thrives` -> `the-squeeze` -> `footfall-shape` -> `adjacent` -> `who` -> `streets` -> `still-filling` -> `method` -> `one-thing` -> `where-next` -> `footer` (chrome)

**Global rules per `00-README.md`** — palette = cool greys + terracotta only (no moss/amber/brown/cocoa/cream); fonts = Geist Sans (body/labels) + Geist Mono (figures, tabular 500) + Newsreader (3 serif moments: district name, honest-take verdict, the closer) + Space Grotesk (the one hero figure); currency = USD where money appears at all (this page withholds it by design); good/bad = calibrated WORD + baseline-relative POSITION, never semantic colour, never pips; bento half-width (col6) default, full-width only for masthead / a genuinely wide table or chart / the closer; viz named from global-standards §6; no chart type more than twice per page; redundancy law (one section per customer job); honesty rails (terracotta only on the subject, unheld folds to ONE strip, "not a league table", districts NEVER scored). **PAGE CONTRACT: EVERYTHING IS RELATIVE — a multiplier vs the 1.0x city baseline; NEVER an absolute money figure at this altitude.**

---

## The section table — ONE ROW PER SUBSECTION, in order, for all 13 approved-reform beats

Beat list authority: `page-architecture/00-APPROVED-REFORM-2026-06-18.md` "NEIGHBOURHOOD — reformed list (13 beats; the squeeze made visible)" (lines 117-131), in that exact order. Each beat is EXPLODED into one row per subsection per `page-guidance/neighbourhood.md` "The full section list (LONG, in order)" and the SPEC §4.2 schema. `Graphic` is a CLOSED set from `00-global-standards.md` §6 (re-skinned to tokens): `two-marker-multiplier-gauge`, `diverging-centre-baseline-bars`, `paired-diverging-bars`, `relative-season-strip`, `two-axis-data-table1`, `POPs-life-stage-donut`, `prime-street-cards`, `sibling-district-tiles`, `stat-tile`, `prose`, or `none`. Exactly ONE named graphic per row. Width per `00-BENTO-REFORM-PLAN` (half/col6 default).

| # | Section (id) | Subsection | Statistic | Graphic | Width | Data | Honesty |
|---|---|---|---|---|---|---|---|
| 1a | Navbar + breadcrumb (`navbar`) | global nav | none (chrome) | `none` | full | nav entities (Countries / Industries / Cities / Compare) + ONE dark CTA + inline search | No data figure. Upgrade the mockup's reduced bar to real `navbar1` dropdowns + search (re-skinned to tokens). |
| 1b | Navbar + breadcrumb (`navbar`) | breadcrumb (FIRST-CLASS upward nav) | none (crumb trail) | `none` | full | `Home / GB / London / All neighbourhoods / West End` (country, city, all-neighbourhoods index, district name) | Breadcrumb is the most-used exit on a relative page that constantly needs the city for context; **city + all-neighbourhoods are the two highest-value crumbs**. Quiet inline eyebrow. No data figure. |
| 2a | District hero (`headline`) | place image + H1 | none (district name) | `none` | full | `district_name` (H1, Newsreader); full-bleed district photo + white-from-bottom gradient scrim | Photo legibility: cream base under the gradient, never white text on a busy photo. Hero-image availability is a founder gate (real photo vs stylized map-motif fallback). No data figure. |
| 2b | District hero (`headline`) | two-marker verdict line + the hero figure | top-trade lift AND most-squeezed trade, as MULTIPLIERS (e.g. "Hotels 2x+" / "Home cleaning 0.7x") | `none` (Newsreader figure beside the gauge; magnitude lives in 2c) | full | `top_trade` + `top_trade_lift`; `most_squeezed_trade` + `squeeze_multiple`; hero figure in Space Grotesk, verdict line in Newsreader | RELATIVE ONLY — the hero figure is a trade-labelled MULTIPLIER, **never a money figure, never a 0-100 district score**. The lift-AND-squeeze pairing is mandatory; showing only the lift is a lie of omission. Verdict line carries the meaning so the gauge stays subordinate. |
| 2c | District hero (`headline`) | the lift-vs-squeeze gauge (Job B instrument) | lift multiple + squeeze multiple on a 0.4x..3.0x track, 1.0x baseline | `two-marker-multiplier-gauge` | full | `top_trade_lift` (terracotta marker, right), `squeeze_multiple` (hollow grey dot, left), gauge domain 0.4x-3.0x, baseline 1.0x, hatched clamp band past ~2.0x | Good = right of the 1.0x fulcrum; squeeze = left. 1.0x pinned in ink does ALL the rating work. Hatched ceiling band past ~2.0x is honest about clamping. The two-marker form (not single-marker) earns its place by previewing the SPREAD the §4 first bar would otherwise duplicate. Districts are NEVER given a /100 score. |
| 2d | District hero (`headline`) | "relative read by design" note + character pills | price tier; economic tags; one character word | `none` (three-weight pill row) | full | `price_tier` (Luxury/Premium/Mid/Value), `economic_tags[]` (Tourist zone / Nightlife / Transit hub), `character_word` | Price-tier pill is the ONLY pill that is itself a good/bad signal (luxury = caution); tags = neutral context; character word = identity, not rating. The note states "relative read by design" so "no money figure" reads as discipline, not a gap. |
| 3a | The honest take (`honest-take`) | verdict + up-to-3 plain points | none (qualitative diagnosis) | `prose` | half | `verdict` (Newsreader, 1 of 3 serif moments); up to 3 `points[]`, ONE district-specific & falsifiable (e.g. "Soho's back streets trade unlike Oxford St minutes away") | Require ONE district-specific, falsifiable point so it never reads as boilerplate. Calm accent panel (cream-tint card, 3px terracotta left rule, ONE accent, narrow measure) — the page's only soft-tinted prose panel. Sanctioned statement #1 of "revenue lift != profit lift" (the expectation reset; phrased distinctly from §5 and §12). |
| 3b | The honest take (`honest-take`) | modeled-not-measured caveat | none | `none` (caption) | half | `caveat` ("modeled, not measured") | States "a lift is not a profit, a West End read is not a London read" — converts "missing money" into deliberate discipline. Caption register. |
| 4a | What thrives here (`thrives`) | the diverging trade picture | 8+ trades, each a lift/squeeze multiple vs 1.0x | `diverging-centre-baseline-bars` | full | per trade: `trade_name`, `lift_multiple` (relative to 1.0x), `is_subject` (lone leader, terracotta), `cell_href` (link-gated); real squeezes carried (grocery 0.85x, doctors 0.8x, home cleaning 0.7x) | The fix for "four identical clamped 2x bars reading as fabricated": diverging grammar (lifts RIGHT, squeezes LEFT of the 1.0x centre) on ONE shared scale, so a 0.7x bar is honestly shorter than a 0.85x bar and real variance proves the model discriminates. **NEVER compute or print a raw percentage** — multiplier + bar position only. Good/bad = side of centre; magnitude = bar length. Rows deep-link into the per-trade cell (link-gate to trusted-local cells only; leave the rest non-clickable). |
| 4b | What thrives here (`thrives`) | one-trade decomposition sub-card | qualitative driver bands (commuter / tourism / character) scoped to ONE trade | `stat-tile` (distinct bordered sub-card, qualitative bands) | full | `scoped_trade`; `commuter_band`, `tourism_band`, `character_band` (qualitative: Moderate/High/Strong) | Scoped to ONE trade on purpose — "never a claim about the district." Qualitative bands, NOT fake "1.00x" parts. A distinct bordered sub-card, not a bar set (kept off the §4a chart count). |
| 5a | The squeeze (`the-squeeze`) — **THE SPINE** | revenue-lift vs rent-lift, paired | `revenue_lift_multiple` vs `rent_lift_multiple` on one shared 1.0x baseline | `paired-diverging-bars` | full | `revenue_lift_multiple` (terracotta bar); `rent_lift_multiple` (ink bar); shared baseline tick 1.0x | The page's HIGHEST-LEVERAGE chart — give it the most vertical room + an accent eyebrow tick marking it as the page's pivot. The good/bad signal IS the GAP between the two bars (pinned together = the lift is illusory). The ONLY paired-bar moment on the page. Still NO money: rent is a relative multiplier vs city, modeled from land character. H2 = "A 2x revenue lift is not a 2x profit lift" (sanctioned statement #2, the visualized proof). **Self-omits** if no honest rent signal AND no price tier is held — then the spine falls back to the §4 diverging bars. |
| 5b | The squeeze (`the-squeeze`) | the "one street back" takeaway | none (qualitative) | `none` (caption/clause) | full | `one_street_back_takeaway` | The actionable clause beneath the bars: the same footfall lifting revenue is the footfall lifting the rent, so a site one block back trades on most of the same crowd for a fraction. Caption register, no figure. |
| 6a | When the footfall arrives (`footfall-shape`) | the week/season shape | relative footfall across the week (or season), busiest/quietest direct-labelled | `relative-season-strip` | half | `footfall_shape[]` (relative weekday-vs-weekend or peak-vs-trough, computed coords, stripped axes, NO money axis); `busiest_label`; `quietest_label` | SUPPORTING, **CUT-IF-THIN — self-omits when uncurated.** The page's ONLY curve (used exactly once). Carries NO money — relative shape only, never a footfall count or covers number. Answers "can I survive the slow start / when do I staff up" (Job E). The breather between the squeeze and the comparison. |
| 7a | Versus next door (`adjacent`) | this district vs up to 3 siblings, multi-row | rep-trade-lift-vs-city, rent-vs-city, area character — across the subject column + siblings | `two-axis-data-table1` | full | `subject_district` (column one, terracotta-tinted, lead mark); up to 3 `siblings[]`; per column: `rep_trade_lift` (vs city), `rent_vs_city` (relative), `area_character` (word); rep-trade cells deep-link | The "should I be one district over instead" answer — the ONLY honest cross-district comparison. Leader mark is ALLOWED here (it is ONE city, a legitimate one-city comparison) but **never crown a PEER in terracotta**. The two-axis design (rent row AND character row) is the explicit fix for "identical clamped 2x rows reading as fabricated": those rows discriminate even when the lift clamps. Zebra `data-table1` in a horizontal-scroll rail. Self-omits when no sibling data held. |
| 8a | Who's here and what it's like (`who`) — MERGE of who + on-the-ground | the demand skew (3 visual-list lines) | who they are / what they spend on / what works (qualitative) | `stat-tile` (`WhatLocalsKnow` visual list, role-distinct line-icons) | half | `skew` (who they are); `spend_on` (what they spend on); `what_works` | SUPPORTING. The demographic skew lives HERE as the WHY behind the diverging bars ("resident demand is thin, so trades needing a local base get squeezed"), **never as a vanity stat tile**. Role-distinct icons (each carries meaning, not decoration). **Curated-only — self-omits** rather than print a vague templated demographic ("affluent evening crowd" is banned). Merges the former thin `ground` beat. |
| 8b | Who's here and what it's like (`who`) | the POPs life-stage donut (the site-selection / POPs layer) | present-on-a-typical-day life-stage mix + per-segment spend vs city median (relative) | `POPs-life-stage-donut` | half | life-stage segments (residents + commuters merged, each counted once; leading life-stage = terracotta lead segment); centre present-count (relative/modeled); per-segment spend read vs city median; tourist `+Nx` visitor multiplier shown APART from the ring | The quantified "who the demand is" instrument backing the qualitative skew line. (a) present-on-a-typical-day mix — tourists kept SEPARATE, the +Nx visitor multiplier shown apart, never inside the ring; (b) centre count + per-segment spend are RELATIVE/modeled reads vs the city median, NOT absolute money; (c) curated/modeled-tagged, self-omits when uncurated; (d) ring = greys + ONE terracotta lead segment — **no moss, no pie elsewhere**. Does not get its own beat (would re-open the who/ground redundancy). |
| 8c | Who's here and what it's like (`who`) | operator's-eye aside | none (qualitative) | `none` (cream-tint Newsreader aside) | half | `operators_eye_aside` (where the margin actually is) | Reframed as operator insight (where the margin actually is, off the main drag), NOT a travel-guide tip. Self-omits when uncurated. |
| 9a | Prime streets (`streets`) | per-street feature cards | per street: name + what it sells + blurb + self-omitting relative rent/spend chips | `prime-street-cards` | half | per street: `street_name`, `what_it_sells`, `blurb`, `rent_chip?` (relative rent-vs-city band, self-omitting), `spend_chip?` (relative, self-omitting), `street_href?` | SUPPORTING, **curated-only** (no card without a real record) — most districts never show it; that honest absence is correct, this is a "London-class" section. The ONE legitimate card grid (content IS discrete places); each card distinct copy/chips, never repeated icon+heading mush. **Chips are RELATIVE / self-omitting — if any chip would be an absolute money figure ($/sqft), it is OMITTED** (the parts-bin mockup's ranked "$78/$70/$58 per sq ft" is out of contract; convert to relative rent-vs-city bands or omit). Each card carries the "one block back trades on most of the same crowd for a fraction" insight. |
| 10a | Still filling in (`still-filling`) | the collapse strip | none (unheld reads as "Not held yet" chips) | `none` (single bordered cream-tint band) | half | `unheld_reads[]` (each a dot + "Not held yet" words: street-by-street character, resident income spread, vacancy/turnover) — never a number | ESSENTIAL scaffolding — reads as discipline ("we will not show a fabricated number"), itself a trust signal. **Absent on a fully-filled district**; present only to absorb genuinely-thin sections into ONE calm band instead of a wall of dashed cards. Never a fabricated number, never a blank. Deliberately the quietest band on the page. |
| 11a | How we get to the number (`method`) | held vs modeled method note (the trust layer) | held / modeled rows, each with a confidence chip | `two-axis-data-table1` (rendered as a labelled two-STATE method NOTE — chip rows, not a TanStack table) | half | `held_rows[]` (curated character, prime streets); `modeled_rows[]` (the multiples, the rent, the shape); `confidence` per row; `method_link`; `as_of_date` ("June 2026") | On a page that shows NO money and only relative multiples, "what is held vs modeled" is exactly the reader's trust question. **Held vs modeled is signalled by a labelled chip (held / modeled), NOT by moss/green** — moss implies kept money this altitude never shows. To keep the heavy `data-table1` grammar to once on the page (beat 7), this beat renders as a chip-row NOTE, not a second TanStack table. Carry the "no absolute money by design" footnote verbatim. Links to the site-wide methodology page (the monetizable trust layer). |
| 12a | One thing to remember (`one-thing`) — the close | the verdict + the actionable forward move | none (qualitative) | `none` (dark ink-900 closer card, terracotta eyebrow, Newsreader verdict) | full | `verdict`; `actionable_move` ("take the catchment, then sign one street back"); `as_of_date`; `flag_it` link | ESSENTIAL. The ONLY dark card on the page, the visual full-stop. Must add a FORWARD instruction the honest-take did not — gives the move, never restating the diagnosis. Sanctioned statement #3 of "revenue lift != profit lift" but carries the ACTIONABLE spin, not just the caveat. "Modeled, June 2026" + "Flag it" are the trust close. Newsreader verdict = 1 of 3 serif moments. |
| 13a | Where to look next (`where-next`) — MERGE of businesses-here + related | sibling-district tiles | none (each a plain character word, NO score) | `sibling-district-tiles` | full | `sibling_districts[]` (name + character word + href, **NO score**) | MERGES the three former link grids (businesses-here, related, the sibling rail) into ONE wayfinding moment — satisfies "more links" without three grids in a row. Sibling tiles are **equal-weight, no scores** (districts are NEVER ranked) — the ONE place equal-weight tiles are correct. |
| 13b | Where to look next (`where-next`) | hand-off tiles + compare CTA | none | `none` (2 hand-off tiles + ONE terracotta CTA) | full | `city_href` (city overview); `all_neighbourhoods_href`; `compare_cta_href` ("Compare two districts") | "Compare two districts" is the page's ONE explicit CTA (district-vs-district is a legitimate one-city comparison). City overview + all-neighbourhoods are the natural zoom-outs. |
| 13c | Footer (`footer`) | rich multi-column footer | none (chrome) | `none` (`footer7`, dark ink-900, re-skinned) | full | footer link columns + newsletter + the modeled-relative disclaimer paragraph | Footer carries the load-bearing relative-money disclaimer ("no absolute money at this altitude by design") — **keep verbatim**. Upgrade the mockup's reduced footer to multi-column. No data figure. |

---

## Footer — conformance config + notes/conflicts

### Conformance config (machine-checkable)

- **Section-id order:** `navbar`, `headline`, `honest-take`, `thrives`, `the-squeeze`, `footfall-shape`, `adjacent`, `who`, `streets`, `still-filling`, `method`, `one-thing`, `where-next`, `footer`. (This IS `PAGE_SECTION_ORDER["neighbourhood"]`, upstream of `src/lib/page-layout/section-order.ts`; the constant currently holds the pre-reform stub `["hero", "neighborhood-industries"]` and must be synced at reform ship, P1.)
- **Currency:** USD ($, abbreviated) **only if any absolute money ever surfaces** — but **this page is RELATIVE-MULTIPLIER-ONLY by contract**: every money read is a multiplier vs the 1.0x city baseline, NEVER an absolute figure at this altitude. USD applies solely as the fallback yardstick should a figure appear below the cell hand-off (it should not).
- **Per-graphic max-count: 2** (no graphic type rendered more than twice across the subsection rows).
- **Banned graphics / treatments:** `absolute-money-on-this-page` (any $ figure, e.g. $/sqft, take-home, rent quote), `pie` (any multi-slice pie — the POPs donut is a single-ring life-stage donut with a terracotta lead segment, NOT a multi-slice pie), `pips` (4-segment rating pips — good/bad is a calibrated WORD + baseline position), plus the site-wide bans: moss/amber/brown/cocoa/cream colour, semantic good/bad colour, raw signed percentages.

### Per-graphic count (the "no chart type more than twice" law)

Counting the `Graphic` column across all subsection rows:

- **`two-marker-multiplier-gauge`** — 2c only = **1**.
- **`diverging-centre-baseline-bars`** — 4a only = **1**.
- **`paired-diverging-bars`** — 5a only = **1** (the ONLY paired-bar moment; do NOT let 4a and 5a both render as a plain ranked-bar set or they collapse into the same chart twice — 5a MUST be the paired two-bar-on-one-track form).
- **`relative-season-strip`** — 6a only = **1** (the page's ONLY curve; do not reuse the area/strip anywhere else).
- **`two-axis-data-table1`** — 7a (the genuine like-for-like zebra table) + 11a (rendered as a labelled chip-row NOTE, not a TanStack table) = **2 at the cap**. **VARY:** keep the heavy `data-table1` grammar to 7a only; 11a is a held/modeled chip-row note. If a future beat needs a third table, fold it into a note or list. Do NOT exceed 2.
- **`POPs-life-stage-donut`** — 8b only = **1** (single-ring, terracotta lead segment, NOT a pie).
- **`prime-street-cards`** — 9a only = **1** (one of the two sanctioned equal-card grids).
- **`sibling-district-tiles`** — 13a only = **1** (the other sanctioned grid; equal-weight by design; different job from prime-street cards — curated texture vs wayfinding).
- **`stat-tile`** — 4b (decomposition sub-card) + 8a (`WhatLocalsKnow` visual list) = **2 at the cap**. These are distinct sub-card / icon-list treatments, not a charted bar type; do not let them proliferate into a third.
- **`prose`** — 3a only = **1** (the calm accent panel; `none` rows that carry caption/aside/closer prose are NOT charted graphics and do not count).

**Spine-grammar watch (NOT a violation):** the baseline-relative bar family appears across 2c (gauge), 4a (diverging bars), 5a (paired bars), and 7a (table-embedded magnitude). These are the page's deliberate L12 through-line but are FOUR distinct instruments (a two-marker gauge = a marker on a track; a full diverging set; a paired two-bar comparison; a table) reading as different chart families per §6, so the ">2x same type" rule is satisfied by visual distinctness — provided 4a and 5a never both collapse to a plain ranked-bar set (5a is paired, 4a is diverging-centre).

### Redundancy / duplicate-job resolution (the redundancy law)

- **Three former end-of-page link grids -> ONE.** `adjacent` (beat 7, *compares* — Job C) stays separate; `businesses-here` and `related` (both pure *navigation* — Job H) MERGE into beat 13 (`where-next`). Compare vs browse are different jobs; only the two that merely link collapse.
- **Two former soft-text beats -> ONE.** `who` and `ground` overlapped (food scene appeared in both); MERGED into beat 8 (`who`, rows 8a-8c). One richer texture block beats two thin ones.
- **Hero gauge vs thrives top bar.** A single-marker hero gauge would be near-redundant with beat 4's first bar. The **two-marker** gauge (2c) earns its place by previewing the *spread* (lift AND squeeze) the single bar hides; beat 4 then carries the full diverging detail. Job B (relative score) vs Job C (per-trade peers) are distinct.
- **"Revenue lift != profit lift" stated in 3a, 5a, 12a.** An *intentional, sanctioned* triple-statement (the honesty contract), NOT a redundancy violation — but each adds a new frame: 3a = the expectation reset (prose), 5a = the visualized proof (the paired bars), 12a = the forward instruction ("sign one street back"). Never copy-paste the same sentence.

### Notes / conflicts (resolved per `00-README.md` precedence) + the parts-bin mockup violations

The standalone parts-bin mockup `E:\atlas\neighbourhood-shoreditch-reform.html` is a **style/component reference only** (cards, gauge track, donut, diverging-bar CSS, footer); its NUMBERS and absolute-money treatment are OUT of contract and are corrected here. The rebuild MUST fix every item below:

1. **Absolute money on this page (HARD breach of the relative-only contract).** The prime-streets area ships a "Rent by street, ranked" panel with **absolute USD rent figures — `$78 / $70 / $58 / $48 / $44 / $36` per sq ft per year** (lines 409-414), and the help tooltip literally says "in USD." This violates the page's relative-only honesty contract (NO absolute money at this altitude). **Fix:** convert beat 9 (9a) rent chips to **relative rent-vs-city bands** (or self-omit); never absolute $/sqft.
2. **Signed percentages instead of multipliers.** The hero/thrives region prints signed percentages — **`+52%`, `+28%`, `+37%`** (lines 225-282). Per APPROVED-REFORM beat 4 and `analysis` fix #1, **never compute or print a raw percentage** — express as a multiplier vs the 1.0x baseline (e.g. "1.5x") and as bar position, not "+52%". **Fix:** every percentage becomes a multiplier + diverging-bar position.
3. **Moss/cocoa -> greys (a guidance-language correction, not a live-token breach).** `page-guidance/neighbourhood.md` and `analysis/neighbourhood.md` repeatedly describe the lift/good end as "green/good," "moss," "atlas," and the squeeze as "cocoa," and the held-vs-modeled note as a "moss chip." `00-global-standards.md` §2.1 still ships moss/amber/cocoa/cream tokens. **RESOLVED** by the 2026-06-23 hard rule + `00-README.md` (lines 16, 24, 61): **cool greys + terracotta ONLY — no moss/amber/brown/cocoa/cream.** Throughout: lift/subject = terracotta; the squeeze and all neutral mass = graded greys; good/bad = baseline-relative POSITION + the calibrated word, **never a green slice, never a moss "held" chip** (use a neutral labelled chip). (Note: the *current Shoreditch mockup already uses greys + terracotta* — no `--moss/amber/cocoa/cream` tokens remain in it — so this is a spec/guidance-language correction to prevent regression, not a live token to strip from that file. This page had the strongest moss/cocoa language of any page type; the correction is load-bearing for any future build that reads the guidance.)
4. **Chart-repetition count (the "no type >2x" law).** See the per-graphic count above: `two-axis-data-table1` = 2 (cap; 11a must be a chip-row note, not a second TanStack table), `stat-tile` = 2 (cap; 4b + 8a, distinct treatments), every other graphic <=1. The paired-bars / diverging-bars / gauge spine reads as four distinct families. **Watch:** never let 4a and 5a both render as a plain ranked-bar set, and never reuse the `relative-season-strip` curve.

### Source citations per beat

- **Beats 1-13, list + order + merges:** `page-architecture/00-APPROVED-REFORM-2026-06-18.md` "NEIGHBOURHOOD — reformed list (13 beats; the squeeze made visible)" (lines 117-131), in that exact order.
- **Per-subsection narrative + honesty + the three movements (the answer 1-3 / the relative engine 4-8 / the texture and the way out 9-13):** `page-guidance/neighbourhood.md` "The full section list (LONG, in order)" §§1-14 (hero §§ lines 9-25; the metric-rating-system lines 21-24; each beat §§1-14; the wayfinding/hand-off four-directions lines 128-136; what-was-wrong + the fixes lines 138-151; founder gates lines 152-156).
- **Reform rationale + what-was-wrong + cuts/merges (the squeeze made the spine, the diverging-bar fix, the two-marker hero, the who+ground merge, the businesses-here+related merge):** `page-architecture/analysis/neighbourhood.md` "Section-by-section audit" (VERDICTs §§0-12) + "What is MISSING" (the squeeze, the seasonality strip, the footfall-vs-spend / POPs note) lines 128-138 + "What to CUT or MERGE" lines 142-148 + the reformed re-ordered list lines 151-167.
- **Viz names + geometry + honesty-per-statistic:** `00-global-standards.md` §6 chart map (lines 211-231: multiplier gauge, score band, gradient area, data-table1, ranked/diverging bars, margin table) + §7 computed-geometry (lines 235-245).
- **Width / bento / the five laws:** `00-BENTO-REFORM-PLAN-2026-06-18.md` §2 (the five laws) + §3 (the bento kit); half/col6 default per `00-README.md` line 28.
- **Palette / fonts / USD / good-bad-as-word / relative-only contract / hero spec:** `page-manifests/00-README.md` (global rules block + reconciliation log lines 60-62) + the precedence table (lines 9-21).
- **POPs life-stage donut placement (inside beat 8) + its honesty notes:** the existing section-level `neighbourhood.md` "The POPs life-stage donut — where it belongs" note + the Shoreditch `.donutwrap` ("Who is around, on a typical day").
- **Nothing-dropped audit:** `docs/brand/section-constitution.md` "NEIGHBOURHOOD page spine" — "Street by street" -> beat 9 (`streets`); "What thrives here and why" -> beat 4; "Who lives and shops here" -> beat 8; "Cost to operate" -> beat 5 (reformed into the squeeze); "Versus next door" -> beat 7; "The businesses here" -> folded into beat 13; "One thing to remember" -> beat 12. **"Operator voices"** is the ONE constitution beat intentionally NOT carried as its own section (per APPROVED-REFORM anti-slop rule #3 + `analysis` "faux operator voices held until real quotes exist") — it survives only as the single operator's-eye aside (8c), a deliberate logged downgrade (held-until-real), not a silent drop.

### Open questions (founder gates — do not resolve unilaterally)

1. **Hero image (2a):** rights-cleared district photo available, or stylized map-motif fallback until imagery is sourced? Gates whether the hero is "alive" or dotted-map.
2. **Cell deep-links (4a & 7a):** are per-trade neighbourhood cells routable for every district, or link-gate to trusted-local cells only (as on country pages)? Determines whether all diverging-bar / table rows are links or a curated subset.
3. **The squeeze rent signal (5a):** is rent-vs-city modeled from land character for ALL districts (then the spine is universal) or curated-only (then 5a self-omits on thin districts and the spine moves to 4a)? Sets the default self-omit behaviour for the page's highest-leverage chart.
