# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (do not commit secrets)
.env*
!.env.local.example
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# IDE
.vscode/
.idea/
.claude/
.superpowers/
design-assets/incoming/

# Ephemeral verification + scratch (added 2026-06-07)
screens/
tsconfig.tsbuildinfo
err.txt
data/audit/_log.txt
data/quality/revenue_clamp_dryrun.csv
*slugcheck*

# city-data pipeline: generated backlog + staged drafts (state.json is tracked)
data/cities/_pipeline/coverage.json
data/cities/_staged/
data/cities/_pipeline/.lock

# brand: raw design-tool exports are staging only (~70MB); the curated assets land in src/ per docs/brand/visual-assets.md
docs/brand/assets/incoming/
# working scratch (quiz trackers etc.); the synthesized docs in docs/brand/ are the source of truth
scratch/

# dev-server logs from local verification runs
.d*.log
.d*.err
.dev*.log
.dev*.err

# Output of scripts/loop/gate.mjs. A run result, not source.
data/loop/
# the static a11y gate's report, a run's output (plan step 14)
scratchpad/audit/
# the harness's compiled stylesheet, rebuilt from src/app/globals.css on every render (plan step 14)
scratchpad/pages/site.css
# the six spine renders the pages-fresh gate writes at the head of the chain, a run's output (plan step 14b)
scratchpad/harness/pages/
