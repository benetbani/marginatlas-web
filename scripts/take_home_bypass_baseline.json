{
  "_note": "Two buckets, not one list. `unreviewed` must be empty: a module that matches the bypass SHAPE and has not been checked is the only thing this gate treats as a failure. `reviewed` holds modules that match the shape and were measured clean, each with the evidence. See the gate header for why a flat count was wrong.",
  "unreviewed": [],
  "reviewed": {
    "src/lib/london/market.ts": "Is the correction itself. Closes the take-home identity at load: 20 of 20 activities agree, and clampMargin moves none of the 20 margins.",
    "src/lib/scores/cell_board.ts": "Reclassified 2026-08-18. It no longer PRINTS anything: the section rows it used to build had no consumer at any of the four call sites and were deleted, taking clampMargin, clampNetMarginPct and every formatted margin with them. It now matches the shape only through net_margin_pct as a FIELD OF THE LondonEntry TYPE, unread here. It receives ownerTakeHome straight from resolveOwnerTakeHome at all four callers and passes it to the rating untouched; on London it prefers the curated owner_take_home, which london/market.ts re-derives from the margin at load. Rating snapshotted over 4,000 input combinations before and after the deletion, byte-identical.",
    "src/lib/spine/adapt_industry.ts": "Matched a separately recomputed buildAcrossCities on 8 rows, worst gap $0.",
    "src/lib/spine/adapt_city.ts": "Matched buildCityActivities on 24 rows across London, New York and Berlin, worst gap $0.",
    "src/app/dev/industry2/page.tsx": "Rendered through react-dom/server against the live builder: 8 of 8 city figures printed exactly as the seed carried them.",
    "src/app/dev/variants/v3/page.tsx": "Dev-only variant harness, robots-disallowed, reachable on no reader-facing route. It DERIVES nothing: it reads owner_take_home, revenue and net_margin_pct straight from LONDON_MARKET, whose closeTakeHomeIdentity re-derives take-home from revenue x margin at load. Measured 2026-08-19 across all 20 curated activities: worst |take_home - revenue*margin/100| = 0 USD. So the three money columns it prints agree with each other by construction upstream and cannot contradict the cell page. Same path cell_board.ts already uses for London, which is itself a reviewed entry above."
  }
}
